# /etc/sudoers.d/dolmus-deploy  (root:root 0440, `visudo -cf` ile doğrulanır)
# `deploy` kullanıcısı yalnız otomatik yayın alıcısını root olarak çalıştırabilir.
Defaults:deploy !requiretty
deploy ALL=(root) NOPASSWD: /usr/local/sbin/dolmus-deploy-receive
