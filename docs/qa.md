# QA

_A field marked **Repos:** applies only to those repositories; a field without the line is project-wide._

## Test Strategy

### Test levels

**Repos:** dolmus-takip

- Unit — Vitest 5.0.1 (project 'unit'): money parsing/rounding, durations, validation, permission decisions, messages, contrast, plate, client state
- Integration — Vitest + real better-sqlite3/Drizzle on a temporary file with real migrations (project 'integration'): composite FKs, transactions, revisions/confirmations, idempotency, SQL reports, multi-connection/process concurrency; mocks or :memory: not accepted for financial proof
- E2E — Playwright 1.63.0, Chromium + WebKit, against the standalone production build with a real test DB
- Ops / load — isolated Linux drills and the target Lightsail in M6: native modules, systemd, WAL, crash/freeze, backup/restore, release, 100-user capacity
- Manual — Android Chrome + iPhone Safari with representative users (tests/e2e/MANUAL-CHECKS.md)

_Integration level kept deliberately: financial integrity must be proven on real SQLite. In the DIJJI runtime container WebKit cannot run, so package gates run Chromium only; full Chromium+WebKit E2E runs in CI and CI must be green before merge (session decision 2026-09-19)._

### Coverage targets

**Repos:** dolmus-takip

- Unit %: no fixed percentage target — coverage reports only locate missing branches
- Acceptance: every one of the 258 acceptance boxes across 35 stories traced as Sx.y/ACn → test or manual evidence
- E2E: all critical flows (login → entry → confirmation → correction → report → staff support) + 28 PRD §9 items mapped to QA01–QA16
- Performance (normal mixed load): save p95 ≤ 2 s, report p95 ≤ 3 s, unexpected errors < 1%
- Integrity: 0 lost financial writes, 0 duplicates, 0 calculation/confirmation mismatches
- Accessibility: text contrast ≥ 4.5:1 (unit-tested), 320 px width and 200% zoom usable
- Lighthouse: no target defined

_QA-PLAN §1 explicitly rejects an arbitrary coverage % or fixed test count; performance targets from ARCHITECTURE §9 / QA-PLAN §3._

## Test Case List

### E2E scenarios

**Repos:** dolmus-takip

| # | Scenario | Steps |
| --- | --- | --- |
| 1 | Vehicle setup and plate login (implemented: vehicle-login.spec.ts, login-accessibility.spec.ts, admin-vehicles.spec.ts, privacy-notice.spec.ts) | Seeded vehicle → /giris → owner password → /sahip; driver password → /sofor; wrong password / unknown plate → same error, form kept; rate limit → 429 message; keyboard/focus/aria checks at 320 px. Staff opens /yonetim/isletmeler/:id/araclar/yeni → creates vehicle → new plate logs in with owner and driver passwords separately; duplicate plate (other spacing/case) → 'Bu plaka zaten kayıtlı.'; identical passwords → field error, nothing created; connection lost during submit → result check, password fields locked, retry with the same passwords → single vehicle; 409 REQUEST_ID_REUSED text with business link; passwords never written to localStorage/sessionStorage; vehicle sessions and anonymous users cannot open the page; staff resets the owner password in 'Şifre sıfırlama' on /yonetim/araclar/:id → success text names plate, 'Mal sahibi şifresi' and WhatsApp, field cleared → old owner password rejected, new one opens /sahip, driver password still opens /sofor; new password equal to the other role's → field error, nothing changed; connection lost during reset → result check with selection and password locked, same password retried → completed; after reload the choice is kept and the password must be re-entered; different password on retry → form-specific REQUEST_ID_REUSED text; reset by another staff context meanwhile → 409, reload → new reset works; new password never in localStorage/sessionStorage; inactive vehicle → section shows the unavailable text. KVKK notice: /aydinlatma-metni opens without a session and without a redirect and shows its title and seven sections; no horizontal scroll at 320 px; /giris and /yonetim/giris each link to it with a target at least 48 px high |
| 2 | Staff login and business management (implemented: platform-login.spec.ts, admin-businesses.spec.ts, admin-vehicles.spec.ts, admin-support-audit.spec.ts (the support area now asserts the 'Özet' and 'Raporlar' links), team-accounts.spec.ts) | /yonetim/giris → admin → /yonetim list → İşletme aç (name + owner) → detail edit → PATCH saved → no-change 422 text → deactivate with confirm → reactivate; support role sees role label; inactive staff rejected. Vehicle detail /yonetim/araclar/:id: brand-model, year, route stop and note edited and kept after reload; stale second tab → 'Bu kayıt değişmiş'; stale local draft replaced by current server values; unknown vehicle → 404. Search on /yonetim: plate typed with other spacing/case or a business name → vehicle cards; no-result text; q/active mirrored in the URL; loading never shows 'Sonuç yok', an error keeps the typed text and 'Tekrar dene' works; 'Daha fazla göster' appends the next page, a new query resets the list and a stale response never overwrites it. Support area /yonetim/araclar/:id/destek shows the target and the real staff identity (admin and support role labels), offers '+ Çalışma kaydı gir' linking to /yonetim/araclar/:id/kayit/yeni but no delivery/report links, unknown vehicle → 404, owner and driver sessions cannot open it; an unsaved field or a typed-but-unsent new password + 'Hedefi değiştir' → confirm dialog, Vazgeç keeps the value, continuing clears the drafts and writes nothing. History /yonetim/islem-gecmisi lists performed actions with the real username and before/after values, never shows passwords, owner/driver cannot open it (API 403); empty, error and 'Daha fazla göster' states are distinct. Team accounts on /yonetim/ekip (admin): an account opened from the UI gets a lower-cased username and a different-case duplicate is refused; full name and role edits persist after reload; a lost response replays the same body after reload (200, version +1 once, single audit rows); a stale tab shows the conflict text and 'Güncel halini aç' loads the server values; deactivation asks for confirmation, ends the account's open session and blocks login, reactivation restores no session; password reset ends the open session, the old password is refused, the new one logs in and the password does not stay on screen; an admin deactivating their own account lands on the login page with the session-ended note; the history shows the target account's username and no password; support sees the unauthorized text, the link is hidden and the API answers 403, owner and driver sessions cannot open the pages; passwords are never written to localStorage/sessionStorage; list, create form and detail are usable at 320 px without horizontal scroll |
| 3 | Daily work entry (implemented T3.1–T3.4 and S3.6: driver-daily-form.spec.ts, owner-staff-work-entry.spec.ts — driver, owner and staff forms with amounts, live summary, saving, the result screen and safe resolution of an unknown result, QA06–QA08) | Implemented (T3.1): driver login → /sofor shows the fixed plate, today's Istanbul date and only active drivers with none preselected; live duration 08:00–17:30 → 9 saat 30 dakika, equal or reversed same-day times → error, 'Bitiş ertesi gün' 22:00–06:00 → 8 saat; missing person or time → error under that field with the other values kept, no request and no saved claim; person no longer selectable on the re-read → field error, list refreshed, times kept; re-read answering 401 → session-ended text, not a person error; a list error never shows the empty-list text and 'Tekrar dene' reloads it; a vehicle without drivers → empty-list text and a disabled picker; no horizontal scroll at 320 px. Implemented (T3.2): with the expense section closed the form has exactly two text inputs (the amount fields) and no number input; 10.000 / 1.500 / 300 + 'Otopark' → live summary 2.000,00 TL share and 6.200,00 TL hand-over before any submit, no 'Kaydedildi', no editable control in the summary; clearing the unused expense → 6.500,00 TL; empty or invalid ('1.5') gross → '—' with no stale total; a note with an empty expense amount → '—'; explicit 0 / 0 → 0,00 TL with no warning; 10.000 / 9.000 / 0 → -1.000,00 TL with the negative hand-over warning; removing a filled expense asks for confirmation (Vazgeç keeps 300, Kaldır clears it), an empty one closes without a dialog; 'Kaydet' with empty gross → 'Tutarı gir. Yoksa 0 yaz.' linked to the field, times and person kept, no driver-list re-read, '1.5' → thousands-separator error; large amounts with the warning at 320 px without horizontal scroll. Implemented (T3.3): only an owner session opens /sahip/kayit/yeni (driver → /sofor, staff → /yonetim, none → /giris); the owner opens it from '+ Çalışma kaydı gir' on /sahip, sees the plate, 'Kendim çalıştım' and 'Şoför adına' both unpressed, no summary and no cash-confirmation control; 'Kendim çalıştım' → 'Çalışan: <owner> · Mal sahibi', 'Şoför payı' 0,00 TL, 'Giderlerden sonra kalan' 8.200,00 TL, the no-share note and no person picker; 'Şoför adına' → 'Şoförü seç' with only active assigned drivers (inactive people, inactive assignments and the owner excluded, same-named people kept as two rows), 'Şoför payı (%20)' 2.000,00 TL and 'Teslim edilecek tutar' 6.200,00 TL; switching kind clears the chosen person; 'Kaydet' without a kind → 'Kayıt türünü seç.' and no request; driver kind without a person → 'Şoförü seç.'. Staff open /yonetim/araclar/:id/kayit/yeni from '+ Çalışma kaydı gir' on the support page: target header and real staff identity shown, every driver-list request carries X-Target-Vehicle for that vehicle, 'Sahip çalıştı' / 'Şoför adına' offered unpressed with the same figures and the 'Sahibin çalışmasında şoför payı ayrılmaz.' note; 'Hedefi değiştir' on a dirty form asks 'Değişiklikleri bırakıp çık?' (Vazgeç keeps the typed amount), a clean form leaves at once; an inactive vehicle shows the inactive-target notice with the form and 'Kaydet' disabled; an unknown vehicle → 404; vehicle sessions are redirected. Implemented (T3.4): driver 'Kaydet' sends exactly one POST with the CSRF header after one fresh driver-list read, then shows 'Kaydedildi' + 'Henüz doğrulanmadı' and clears the draft; a lost response locks the fields and 'Sonucu şimdi kontrol et' re-sends the same requestId and the same body, ending with one entry; a 5xx is treated as unknown and the check saves with the same body; after an unknown result, checks answered 401 or 404 (including the automatic check after a reload) keep the draft locked with the same requestId and body and still end with one entry; definite errors on a first attempt release the form — 422 as a field error, 403 and 409 with their own texts, never 'Kaydedildi'; typed fields survive a reload and are cleared by logout and a new login. Owner: 'Kendim çalıştım' saves with one POST and shows 'Kaydedildi' + 'Onay gerekmiyor'; 'Şoför adına' without a person → error, with a person → fresh read, saved, 'Henüz doğrulanmadı'; a lost response locks the form with 'Kaydın sonucu kontrol ediliyor.' and no 'Başka bir çalışma kaydı gir', and after a reload the automatic check sends the same body and requestId, ending with one entry. Staff: 'Sahip çalıştı' is sent with X-Target-Vehicle and shows 'Onay gerekmiyor'; changing the target clears the draft. Implemented (S3.6): the driver result shows 'Kaydedildi' with focus, '<name> · <plate>', '<date> · 08:00–17:30', the hand-over amount, 'Mal sahibi parayı aldığında burada görebileceksin.' and 'Kaydı aç', with no 'Teslim ettim' / 'Onaya gönder' action; an unknown result shows 'Kaydın sonucu kontrol ediliyor.' in a status region and hides 'Başka bir çalışma kaydı gir'; after a reload the unknown result is checked once automatically — resolved with the same requestId when the connection is back, otherwise still locked with 'Sonucu şimdi kontrol et' enabled and no driver-list read; a double tap sends one POST even with a slow response; offline 'Kaydet' sends nothing, shows 'Bağlantı yok. Henüz kaydedilmedi.' and keeps every field, then saves once when back online; pressing 'Sonucu şimdi kontrol et' offline shows 'Bağlantı yok. Sonuç bağlantı gelince kontrol edilecek.' (never 'Henüz kaydedilmedi') and the online event re-sends the same body; a 401 shows 'Yeniden giriş yap' linking to /giris and logging in to the same vehicle resolves the pending entry with one record; another vehicle's login neither sees nor sends the pending draft; 'Yenile' re-reads the entry with GET, a failed read shows 'Güncel kayıt okunamadı. Tekrar dene.' without claiming deletion and keeps the result; at 320 px neither the form nor the result scrolls horizontally with a long name and a large amount, controls are ≥ 48 px and 'Kaydet' ≥ 56 px. Owner and staff results show '<name> · <plate>' and '<date> · <time range>'; the owner's own entry shows neither the driver hint nor a hand-over action. Implemented (T4.6): after the owner confirms the saved driver entry at 6.000 in a second session, 'Yenile' on the driver's result shows 'Teslim doğrulandı' once, 'Teslim edilecek tutar' 6.200,00 TL and 'Alınan tutar' 6.000,00 TL on separate lines and 'Doğrulama zamanı', and removes 'Henüz doğrulanmadı' and the hint; a failed 'Yenile' shows 'Güncel kayıt okunamadı. Tekrar dene.' and keeps the confirmed state and amounts. |
| 4 | Cash confirmation and correct-and-confirm (unconfirmed edit implemented T3.6, first confirmation implemented T4.2 and correct-and-confirm implemented T4.3, read-only history and staff on-behalf confirmation and correction and the driver delivery status (T4.6) implemented: work-entry-edit.spec.ts, QA09–QA10) | Implemented (T3.6): the owner saves a driver-kind entry and opens it with 'Kaydı aç' → 'Kayıt detayı' shows 2.000,00 TL share, 6.200,00 TL hand-over, 'Henüz doğrulanmadı' and 'Sürüm 1', no kind buttons and no received-amount field → gross changed → 'Değişiklikleri kaydet' → the server's new version, share and hand-over, still 'Henüz doğrulanmadı'; the owner's own entry keeps 0,00 TL share and 'Onay gerekmiyor' after editing and has no person picker; another device saves the same version first → conflict text, current values and the user's draft side by side, fields locked until the user chooses; 'Güncel değerleri yükle' drops the draft and fills the current entry; a save in another tab of the same browser never leaves this tab's stale version saveable; a lost response locks the form and after a reload the retry sends the same body and requestId, ending with one new version; an unknown entry → 404, a driver session is sent to /sofor, staff to /yonetim, no session to /giris; the driver saves today's entry, picks the person on 'Kayıtlarım', lists that person's entries and edits only today's pending entry while an older one is read-only; the driver cannot open the owner's own entry (404) and an owner session on /sofor/kayitlar goes to /sahip; staff save on behalf of the owner, open the entry with 'Kaydı aç', edit with X-Target-Vehicle and CSRF, the edit appears in the audit history and 'Hedefi değiştir' clears the edit draft; unknown entry and unknown vehicle → 404. Implemented (T4.2): the owner saves a driver-kind entry and opens it → '<person> · <plate>', '<date> · 08:00–17:00', 'Henüz doğrulanmadı', Hasılat 10.000,00 / Mazot 1.500,00 / Diğer masraf 300,00 / Şoför payı 2.000,00 / 'Beklenen teslim' 6.200,00 TL and 'Aldığım tutar (TL)' prefilled 6.200,00; a reload sends no confirmation and keeps 'Henüz doğrulanmadı'; typing 6.000 shows 'Beklenenden 200,00 TL az.', 6.200 hides it; a double tap on 'Parayı aldım, tutar doğru' sends one POST { requestId, version 1, receivedCents '620000' } → 'Teslim doğrulandı', 'Alınan tutar' 6.200,00 TL and 'Doğrulama zamanı', no received field, no confirm button and no 'Kaydı düzenle'; -5 → 'Tutar eksi olamaz.' and no request, 6.000 → receivedCents '600000'; a request that never reached the server stays unresolved and after a reload is sent again with the same requestId and body; a lost response after a server-side confirmation replays the same body and ends with one confirmation; another device's edit → the canonical 409 text and the current entry, then a confirmation with the new version succeeds; a negative expected hand-over shows the warning with an empty field and an empty field sends nothing; the owner's own entry has no confirmation; no horizontal scroll at 320 px. Implemented (T4.3): on a confirmed driver-kind entry the owner opens 'Kaydı düzenle' → 'Onaylanmış kaydı düzelt' with 'Aldığım tutar (TL)' prefilled with the confirmed amount, which does not follow a gross change; 'Vazgeç' sends no request; changing only the received amount to 6.100 sends exactly one POST and shows the server's amount, with no horizontal scroll at 320 px; correcting daily fields keeps the received amount and shows the new expected hand-over from the server; another device corrected first → the canonical 409 text and the current values, then a correction on the current values is sent with the new version; a lost response after server-side processing locks the form and after a reload the retry sends the same requestId and a byte-identical body, ending with one new version; the driver view shows no correction control on a confirmed entry, while the staff view shows exactly one 'Düzelt ve onayla' with 'Sahip adına alınan tutar' and no 'Aldığım tutar'. Implemented (history): after a confirmation at 6.000 and a correction to 6.100 the owner opens 'Geçmişi gör' → /sahip/kayitlar/:id/gecmis with the 'Kayıt geçmişi' heading; the first row is 'Kayıt oluşturuldu' with 'Sürüm 1'; exactly two 'Teslim doğrulandı' rows — 6.000,00 TL by 'Sahip oturumu' without 'Güncel' and 6.100,00 TL marked 'Güncel'; the current summary shows 6.100,00 TL; no edit, delete, save or correct button and no 'toplam' text; no horizontal scroll; a driver session opening the owner or staff history URL is sent to /sofor and its own detail page shows no 'Geçmişi gör' link; an unknown entry id → 404. Implemented (staff on-behalf confirmation): staff open the owner's pending driver-kind entry on /yonetim/araclar/:id/kayitlar/:entryId → target header with 'İşlemi yapan: <username>', 'Sahip adına alınan tutar' and no 'Aldığım tutar' / 'Parayı aldım'; 6.000 + 'Sahip adına teslimi onayla' sends exactly one POST /confirm with X-Target-Vehicle = the URL vehicle, the CSRF header and receivedCents '600000' → 'Alınan tutar' 6.000,00 TL with gross, share and hand-over unchanged; 'Sahip adına platform desteği · <username>' appears on the staff and owner detail pages and on both history pages; staff then 'Düzelt ve onayla' with 6.100 sends one POST /correct-and-confirm with X-Target-Vehicle and receivedCents '610000'; a typed amount + 'Hedefi değiştir' → 'Bırakıp çık' clears the draft and the field returns prefilled with 6.200,00; the same entry under another vehicle's staff URL → 404 Implemented (T4.6, driver delivery status): the driver's /sofor/kayitlar/:id for a pending entry shows 'Henüz doğrulanmadı' once, the hint and 6.200,00 TL, with no 'Alınan tutar', confirm, correct, edit or history control and no horizontal scroll; no entry read is sent until 'Yenile'; after the owner confirms 6.000 one 'Yenile' read shows 'Teslim doğrulandı', expected 6.200,00 TL and received 6.000,00 TL on separate rows and 'Doğrulama zamanı'; a failed read keeps the last known state; after the owner's correct-and-confirm (gross 12.000, received 6.100) 'Yenile' shows only the new 7.800,00 TL / 6.100,00 TL; after a staff on-behalf confirmation the driver sees 'Sahip adına platform desteği' without the username and the /sofor/kayitlar row says 'Teslim doğrulandı', never 'Onaylandı'. |
| 5 | Period reports (vehicle and person period reports, the day-by-day tab and the owner summary implemented: owner-reports.spec.ts, owner-summary.spec.ts, and the staff support mode: staff-reports.spec.ts, QA11) | Implemented: only the owner opens /sahip/raporlar (driver → /sofor, staff → /yonetim, no session → /giris) and /sahip links 'Raporlar'; an empty month shows 'Bu dönemde kayıt yok.' and no amount; after a driver and an owner entry the month shows 14.400,00 TL remainder and 0,00 TL received, 6.200,00 TL after the confirmation and 6.000,00 TL after correct-and-confirm; 'Hesap dökümünü gör' shows 20.000 gross, 3.000 fuel, 600 other expense, 2.000 share, 19 hours and 1 day; the 'Kişiler' tab shows two cards (driver and owner), each '9 saat 30 dakika · 1 gün · 1 çalışma', the owner card labelled 'Mal sahibi' with 'Pay 0,00 TL', while the breakdown keeps 19 hours; the driver's detail shows Mazot 1.500,00 TL, Diğer masraf 300,00 TL and Hesaplanan kalan 6.200,00 TL and one 'Kaydı aç' that opens /sahip/kayitlar/:id; the 'Gün gün' tab is shown unselected next to 'Kişiler' and the summary is labelled as the whole period's vehicle total; no forbidden wording; previous/next re-read the API and show the server's range; week and year buttons send their period; loading shows no amount, a failure shows the text and 'Tekrar dene', a 401 shows the log-in-again text; a fast tab switch never lets an old response overwrite the new period; a negative remainder keeps its minus sign and a total above 2^53 cents is shown exactly; no horizontal scroll at 320 px. With mocked responses the persons tab shows no card, amount or empty text while loading or after an error and 'Tekrar dene' recovers; a malformed body counts as an error, a 401 shows the log-in-again link and a successful empty response shows 'Bu dönemde kişi kaydı yok.'; a slow response for an earlier person never appears under a newly opened person and changing the period closes the open detail; 'Daha fazla göster' appends the same person's next page; a long name wraps with no horizontal scroll at 320 px in the list and the detail. With mocked responses the 'Gün gün' tab first requests only period and date (no workerPersonId, status or cursor) and shows three cards — the confirmed one with 'Teslim doğrulandı', 'Hasılat', 'Teslim edilecek tutar' and 'Teslim alınan', the pending one without 'Teslim alınan', the owner one with 'Onay gerekmiyor', 'Giderlerden sonra kalan' and a 'Kaydı aç' link to /sahip/kayitlar/:id; person and status filters re-read only the list while the summary amounts stay and the vehicle report is not re-requested; previous/next re-read the summary and the list and reset filters and paging; 'Daha fazla göster' appends the next page, a 500 keeps the cards and 'Tekrar dene' resends the same cursor; a delayed response of an old filter never appears after the new one and a pending next page is not appended; loading, 401 and empty are separate states and the empty text appears only after a successful empty response; a long name wraps with no horizontal scroll at 320 px. Owner summary (implemented: owner-summary.spec.ts): /sahip shows '<plate> · <owner>', the 'Sahip bağlantıları' tabs 'Raporlar' → /sahip/raporlar and 'Şoförlerim' → /sahip/soforler, '+ Çalışma kaydı gir' → /sahip/kayit/yeni, 'Çıkış', the current month's range and 'Bu dönemde kayıt yok.' with no amount; no horizontal scroll at 320 px; a driver session opening /sahip lands on /sofor. After a driver and an owner entry the summary shows Hasılat 20.000,00 TL, Mazot 3.000,00 TL, Diğer masraf 600,00 TL, Şoför payı 2.000,00 TL, Hesaplanan kalan 14.400,00 TL and 'Teslim alınan (onaylı)' 0,00 TL; the pending list shows only the driver entry with 'Henüz doğrulanmadı' and 'Kaydı aç' → /sahip/kayitlar/:id (the owner entry is not listed); opening it confirms nothing; after a 6.000 confirmation going back re-reads the summary → received 6.000,00 TL, remainder unchanged, 'Bu dönemde bekleyen kayıt yok.'; no forbidden wording. With mocked responses loading shows no amount; a summary failure shows 'Rapor yüklenemedi. Tekrar dene.' without the server message or any amount; a 401 shows the log-in-again link; a pending-list failure keeps the totals; a fast week → year switch never lets the slow week response overwrite the year range and the pending list requests the server's start day with status=pending. Staff support mode (implemented: staff-reports.spec.ts, 5 tests; the test opens its own business and vehicle, the owner session writes the entries and staff only read): /yonetim/araclar/:id/ozet and /raporlar open only for staff — owner → /sahip, driver → /sofor, no session → /yonetim/giris, unknown vehicle → 404; the summary shows the support target header and the PRD totals identical to the owner view, every report and list request carries X-Target-Vehicle and 'Kaydı aç' goes to /yonetim/araclar/:id/kayitlar/:entryId; the reports page shows the same remainder and received amount as the owner report and the 'Kişiler', person detail and 'Gün gün' links go to the staff entry pages; switching the target never shows the previous vehicle's amounts, a back/forward-cache return re-reads and no amount remains after logout; at 320 px there is no horizontal scroll and the period tabs and filters stay at least 48 px high. Planned: renamed/inactive person history in the browser (covered today by the integration test) |
| 6 | Logout and access revocation (vehicle deactivation implemented in admin-vehicles.spec.ts; vehicle password reset revoking only that access's sessions covered in reset-vehicle-password-route.test.ts; rest in integration; E2E per S1.4/S2.3) | Login → Çıkış → protected page redirects to login and local draft cleared; staff deactivates vehicle on /yonetim/araclar/:id after ConfirmDialog → login with its passwords rejected, vehicle still reachable from the business page; staff resets vehicle password / deactivates business → open vehicle session gets 'Oturumun sona erdi' on next action |
| 7 | Daily backup, restore and release (M6 ops drill, QA13–QA15) | Build tarball in CI → release:verify → install on isolated Linux → migrate → smoke (health, login, one entry, confirmation, report) → Backup API copy + verify → restore to new machine → totals 14.400/6.000 and login verified, restored sessions revoked; failed migration → rollback before traffic |
| 8 | Driver management — Şoförlerim (implemented S2.4: drivers.spec.ts; KVKK name anonymisation: person-anonymize.spec.ts) | Staff creates a business with two vehicles → owner of vehicle A opens 'Şoförlerim' from /sahip (empty-list text; no 'Tüm araçlarda' control) → '+ Şoför ekle' 'Ali Veli' → row appears → 'Düzenle' shows the rename note → renamed → 'Bu araçta pasife al' shows the shared driver password warning and empties the list → 'Pasif şoförleri göster' → 'Yeniden aktifleştir'. Owner of vehicle B types 'ali veli' → similar-name hint → 'Bu araca bağla' links the same person (no duplicate), candidate list disappears. Staff on /yonetim/araclar/:id/soforler sees plate · business · owner and username; 'Şifre sıfırla' links to #sifre-sifirlama; 'Tüm araçlarda pasife al' dialog lists both plates → 'Kişi tüm araçlarda pasife alındı.' → 'Kişiyi yeniden aktifleştir' offered; owner list empty after reload. Two same-named seeded people stay two rows; /sahip keeps its heading and text and gains the Şoförlerim link; anonymous, driver and owner sessions cannot open pages they are not allowed to. KVKK: an admin on /yonetim/araclar/:id/soforler confirms 'Adı anonimleştir' → the row shows the anonymous name and the note, with neither rename nor anonymisation left, and the owner screen offers no action; a network error leaves the result unknown and 'Tekrar kontrol et' re-sends the same requestId and body after a reload; in the business owner section a 5xx is retried with the same body and the anonymised owner name can no longer be edited; a support user sees the action on neither screen |

_One row per architecture flow (7 flows ↔ rows 1–7); row 8 covers driver management (S2.4), whose flow card is an open question in this draft. Currently 8 E2E spec files exist (vehicle-login, platform-login, admin-businesses, admin-vehicles, login-accessibility, drivers, admin-support-audit, team-accounts). Search, support area and audit history are added to row 2 because they extend the staff flow; that flow card is an open question in this change. drivers.spec.ts now expects '<username> · Yönetici' because the staff identity also appears in the support target header. Team accounts (S2.6) are added to row 2 as well; row 6 (access revocation) is unchanged because team-account revocation is proven inside team-accounts.spec.ts. T3.1 added tests/e2e/driver-daily-form.spec.ts (9 E2E spec files now) and partly implements row 3; the amounts, save and offline retry steps stay planned. vehicle-login.spec.ts now expects the 'Günlük kayıt' heading and the 'Kim çalıştı?' picker on /sofor instead of the placeholder text; platform-login.spec.ts checks '<username> · <role label>' inside the page header only. T3.2 extended driver-daily-form.spec.ts (still 9 E2E spec files) with the amount fields and live summary in row 3; the existing T3.1 cases now fill gross and fuel before 'Kontrol et'. Save and offline retry stay planned. T3.3 added tests/e2e/owner-staff-work-entry.spec.ts (10 E2E spec files now) to row 3 for the owner and staff forms; admin-support-audit.spec.ts now expects the '+ Çalışma kaydı gir' link on the support page and still no delivery/report links (row 2). T3.4 extended driver-daily-form.spec.ts and owner-staff-work-entry.spec.ts (still 10 E2E spec files) with saving, the unknown-result lock and retry, reload persistence and the definite-error release rules; the earlier 'Kontrol et' / not-saved cases were rewritten around 'Kaydet'. Row 3 is now fully implemented. T3.6 added tests/e2e/work-entry-edit.spec.ts (11 E2E spec files now) to row 4 for editing unconfirmed entries, the driver's K1 list and edit window, conflict handling and the unknown-result retry; confirmation and correct-and-confirm in row 4 stay planned. This task (S3.6, labelled T3.7 in the spec titles) extended driver-daily-form.spec.ts and owner-staff-work-entry.spec.ts (still 11 E2E spec files) in row 3 with the result screen, the automatic and manual resolution of an unknown result, offline handling, the double-tap guard, the 401 login link, per-vehicle draft isolation, 'Yenile' and the 320 px / touch-size checks; the T3.4 cases now use 'Sonucu şimdi kontrol et' and 'Kaydın sonucu kontrol ediliyor.' instead of the removed 'Kaydı tekrar dene' and unknown-result texts. T4.1/T4.2 extended work-entry-edit.spec.ts (still 11 E2E spec files) in row 4 with six owner cash-confirmation cases; the existing owner cases now open the edit form with 'Kaydı düzenle' and read 'Beklenen teslim' in the owner summary. Staff confirmation is covered only by the integration test, because the staff page has no confirmation panel. T4.3 extended work-entry-edit.spec.ts (still 11 E2E spec files) in row 4 with five correct-and-confirm cases for the owner plus the driver/staff no-control check; staff correct-and-confirm is covered only by the integration test, because the staff page has no correction form. The work-entry history change extended work-entry-edit.spec.ts (still 11 E2E spec files) in row 4 with one owner history case incl. the driver redirect and no-link checks; staff history reads are covered only by the integration test. The staff on-behalf confirmation change extended work-entry-edit.spec.ts (still 11 E2E spec files) in row 4: the former staff no-correction check now expects one 'Düzelt ve onayla' with the on-behalf label, and two staff cases cover the on-behalf confirmation + correction with X-Target-Vehicle and the support trace on the owner/staff detail and history pages, and 'Hedefi değiştir' with a typed amount plus the other-vehicle 404. Staff confirmation and correction are therefore no longer covered only by the integration tests. The driver delivery-status change (T4.6) extended driver-daily-form.spec.ts in row 3 (result screen: the owner confirms in a second session, 'Yenile' shows the received amount apart from the expected one, a failed read keeps it) and work-entry-edit.spec.ts in row 4 with two driver cases (delivery status on /sofor/kayitlar/:id with no polling, 'Yenile', failure and correct-and-confirm; the staff on-behalf trace without username and the list label); still 11 E2E spec files. The vehicle period report change added tests/e2e/owner-reports.spec.ts to row 5 (6 tests); the remaining row 5 steps stay planned; 12 E2E spec files now. The person period report change extended owner-reports.spec.ts in row 5 (6 → 11 tests: the persons tab and detail in the real-data flow, plus a mocked-response group for loading/error, malformed/401/empty, stale person or period responses, 'Daha fazla göster' and 320 px); still 12 E2E spec files. The daily-entries change extended owner-reports.spec.ts in row 5 (11 → 18 tests: the real-data flow now asserts the unselected 'Gün gün' tab and the whole-period summary note, plus a mocked-response 'Gün gün' group of 7 tests); still 12 E2E spec files. The owner summary change added tests/e2e/owner-summary.spec.ts (4 tests) to row 5 — 13 E2E spec files now; vehicle-login.spec.ts (row 1) and drivers.spec.ts (row 8) now expect the summary (attached 'Özet' heading, '+ Çalışma kaydı gir', 'Dönem' select) instead of the placeholder text, and owner-reports.spec.ts waits for the summary's own requests to settle before installing its mocks. The staff summary and reports change added tests/e2e/staff-reports.spec.ts (5 tests) to row 5 — 14 E2E spec files now — and changed the support-area test in admin-support-audit.spec.ts (row 2) from 'no report or summary link' to asserting the 'Özet' and 'Raporlar' hrefs while still expecting no delivery-confirmation link. The KVKK change added tests/e2e/privacy-notice.spec.ts (row 1) and tests/e2e/person-anonymize.spec.ts (row 8) — 16 E2E spec files now; both sit under the Playwright testDir ./tests/e2e, so CI runs them without a pipeline change._

### Unit coverage

**Repos:** dolmus-takip

- Caddy — deploy-config.test.ts (unit, static read of deploy/caddy/Caddyfile): single {$DOLMUS_DOMAIN} site, reverse_proxy only to 127.0.0.1:3000 with no file serving, no HTTPS-disabling option, http:// address or trusted_proxies, /api/v1/health/* 404 placed before reverse_proxy; access log inside the site block to stdout with headers deleted, the global options block carries only the named error log including http.log.error, both filters delete/mask the same fields (headers, q, cursor, remote_ip/client_ip /16 and /32, remote_port) and no action restores headers; live headers/TLS still verified in the M6 ops drill
- Next.js app / HTTP layer (src/server/http) — error envelope, protected-route wrapper: errors.test.ts, handler.test.ts
- Identity/authorization (src/server/auth) — cookie, session, app-origin, permissions, scope, rate-limit, hash-queue unit tests; vehicle-password.test.ts (Argon2id hash/verify, random salt, owner≠driver rule); session-* / scope-* / protected-route integration tests; login/deactivation race (session-usecases.test.ts, vehicle-login-route.test.ts: target inactive at session insert → 401 INVALID_CREDENTIALS, no session row; login/password-reset race: credential reset right after Argon2 verification → 401 INVALID_CREDENTIALS, no session — reset-vehicle-password-route.test.ts); staff login/deactivation and login/reset race: createPlatformSession throws PlatformSessionTargetInactiveError / PlatformCredentialVersionChangedError and writes no session row, issued_version equals the current version on success (session-usecases.test.ts), login right after a reset or deactivation → 401 INVALID_CREDENTIALS, no session row (admin-users-routes.test.ts); 429 log lines (platform-login-route.test.ts, vehicle-login-route.test.ts, vehicle-login-hash-queue.test.ts): exactly one line per RATE_LIMITED / HASH_QUEUE_FULL response carrying request_id (plus hash queue fields on HASH_QUEUE_FULL) and never the plate, username, IP or password; beyond 4 concurrent + 100 waiting hashes vehicle logins get 429 HASH_QUEUE_FULL while those within capacity end in 401; permissions.test.ts: 4 actors × 17 permissions, person.anonymize held by admin only; session-scope-summary.test.ts: the admin session returns every permission incl. person.anonymize; hash-queue.test.ts takeHashQueueIntervalStats: all zero on an empty queue, counts only executed verifications (return value and fn error pass through unchanged; none counted when HashQueueFullError means fn never ran), reports max pending and the longest wait of dequeued and timed-out requests, a request still waiting at read time is reported in both that interval and the next
- Receipts module — replay vs 409 REQUEST_ID_REUSED, scope key: mutation-receipts-scope.test.ts (integration)
- Admin module — business create/update/owner/deactivate: admin-businesses-routes.test.ts, platform-admin-cli.test.ts; vehicle create/list/detail/update/deactivate: admin-vehicles-routes.test.ts (single-transaction write, plate conflict, identical passwords 422, request_id replay incl. real concurrent POSTs and different-password 409, version conflict, no-change 422, deactivation revokes sessions and reactivation does not restore them, 403 for vehicle sessions); vehicle password reset: reset-vehicle-password-route.test.ts (200 updates hash + credential_version and revokes only the target credential's sessions, same request_id + same password replays without a second reset, different password 409 REQUEST_ID_REUSED, other role's password 422, unknown vehicle 404, inactive vehicle 403, vehicle sessions 403, real concurrent owner+driver reset to the same password leaves only one working, mid-transaction failure rolls back hash/version/sessions/audit/receipt); business/vehicle list search and paging: admin-list-search-routes.test.ts (existing keys kept and business/owner/active added, plate found in any spacing/case, Turkish case folding on business and owner names, LIKE wildcards treated as text, active filter and nextCursor paging without repeats incl. filtered results, newest business first, invalid q/active/cursor/limit 422, vehicle sessions 403); audit read: admin-audit-route.test.ts (401 without session, 403 for owner and driver sessions, 200 for support and admin, GET-only route, real actor and before = null on create rows written through the real routes, no password/hash/token/cookie/csrf key, session id or raw password in the response, request_id replay adds no second row, actor username resolved after all sessions are deleted, unknown business or other business's vehicle 404, invalid uuid/limit/cursor 422, businessId/vehicleId narrowing with business-less rows only unfiltered, cursor paging returns every row exactly once incl. equal occurred_at and no repeats when rows arrive between pages) and sanitize.test.ts (unit: case-insensitive nested secret-key stripping, harmless keys kept, malformed or non-object JSON → {}); team accounts: admin-users-routes.test.ts (support, owner and driver sessions 403 on every endpoint with nothing changed; list incl. inactive without secret fields, unknown id 404; create with a canonical lower-case username, audit + receipt, no secret leak, the new account can log in; duplicate username in any case incl. a mixed-case CLI row 422 with nothing written; invalid role, username and full name 422; same request_id + same password replays without a second account, different password 409 REQUEST_ID_REUSED; active:false revokes the account's sessions in the same transaction and reactivation restores none, version and conflict rules; name and role changes write audit without bumping credential_version or revoking sessions; a demoted admin's next request 403 with the session kept; role TOCTOU between guard and write 403 with rollback; the last active admin cannot deactivate or demote itself or be deactivated 422; mid-transaction failure changes neither platform_users, sessions, admin_audit nor mutation_receipts; reset changes the hash, revokes sessions, keeps version, returns id + username only and audit carries no secret; reset replay makes no second reset, different target 409 REQUEST_ID_REUSED, unknown account 404; a self-reset revokes the admin's own session and still responds; audit rows carry targetUser even after the account is deactivated, null on other rows); admin-hash-queue-log.test.ts (integration): HASH_QUEUE_FULL maps to 429 with exactly one log line carrying request_id and queue metrics, other error classes write no log
- Drivers module (src/server/usecases/drivers) — drivers-routes.test.ts (integration): GET gives driver sessions only active person + active assignment with personId/fullName, query params cannot widen it, owner rows and globally inactive people excluded, owner/staff get the management view with candidates (owners and other businesses excluded), staff without target header → 422; driver sessions 403 on POST/PATCH/PUT with no row changed; POST writes one person + one assignment + 2 audit rows + receipt with normalized name and the real actor (vehicle_credential, or platform_user on behalf of the vehicle owner), replay vs 409 REQUEST_ID_REUSED, invalid bodies 422 with nothing written; PUT re-opens and closes the same row without duplicates and without revoking driver sessions, stale/missing version 409, same value 422, parallel links → one 201 one 409, no-row active:false 422 / version 409, other-business/unknown person and other vehicle in URL 404, owner person 422, globally inactive person 422 (closing allowed); PATCH rename keeps the id, bumps version and audits before/after, owner sending active → 403, stale 409, same name 422, owner persons and unlinked people 404, staff active:false changes only people.active and returns affected vehicles; drivers-affected-vehicles.test.ts: batch lookup equals per-person lookup, empty input runs no query, no cross-business leak; person-anonymize-route.test.ts (integration): admin 200 replaces the name with the id-derived anonymous name, sets anonymized_at and bumps version with no row deleted or rewritten; the new audit row never carries the old name while the earlier rename row keeps it; period totals identical before and after; support, owner and driver sessions 403, no session 401; other-business person 404 PERSON_NOT_FOUND, unknown business 404 TARGET_BUSINESS_NOT_FOUND; stale version 409 VERSION_CONFLICT; a second anonymisation 409 PERSON_ANONYMIZED with its Turkish text; replay 200 without a second write, different body 409 REQUEST_ID_REUSED; scope or extra body keys and a missing or invalid version 422; renaming an anonymised person through PATCH /drivers (owner and staff) or ownerRename 409 PERSON_ANONYMIZED while an activity change still works; person-name.test.ts (unit): anonymousFullName comes only from the first 6 id characters upper-cased, is stable per id and passes full-name validation
- Work/delivery module (src/server/usecases/work-entries) — work-entries.test.ts (implemented T3.2): driver 08:00–17:30 with 10.000 / 1.500 / 300 → 570 min, share 200000, remainder 620000, calculation version 1; owner share 0, remainder 820000; share, remainder, shareBps, workKind, calculationVersion and duration sent in the body ignored; negative remainder returned as is; unused other expense → 0 and null note; a note with an empty amount → amount error; note up to 200 chars after trimming, longer or non-string refused; gross and fuel required, explicit 0 valid; cent strings only ^(0|[1-9][0-9]*)$; above MAX_SAFE_INTEGER → too-large error; expenses + share beyond the safe limit → field error; time-rule errors keyed by field, overnight 240 min; non-object body refused; the schema carries no personId (scopeSafeObject). subject.test.ts (implemented T3.3): driver type with a person id accepted, owner type valid without a person; client-sent personId, businessId, role, workKind and shareBps stripped; missing, unknown, numeric or empty workType → 'Kayıt türünü seç.'; a workerPersonId longer than 64 chars or not a string refused; the schema never uses the personId name (scopeSafeObject). work-entry-prepare-create.test.ts (integration, implemented T3.3): driver session with owner type → 403; driver session naming the owner person → 422; owner session with owner type → the vehicle's owner person, share 0, not_required, vehicle-credential actor; owner session with an assigned active driver → 2000 bps, pending; staff session → real platform-user actor on behalf of the owner; the same driver body gives the same figures in driver, owner and staff sessions; other-business, inactive person, inactive assignment, unknown id and missing person → 422 with no row written; invalid workType → 422 and figure errors returned together with the person error; client-sent personId, businessId, role, workKind and shareBps change nothing; readVehicleOwnerPerson returns the scoped vehicle's owner. work-entries-create-route.test.ts (integration, implemented T3.4): driver session → 201, one entry (pending, version 1) + revision v1 + receipt, no audit and no cash confirmation; owner kind → not_required, share 0, no audit; staff session → exactly one admin_audit row (platform_user, on behalf of the owner) and the same actor on the revision; client-sent share, businessId and status dropped; same requestId + same content replays the same entry and status without a second row; the hash is over normalized content (extra fields, whitespace and an owner-kind workerPersonId do not count); 100 concurrent identical requests → one entry, all 201 with the same id; same requestId + different body → 409 REQUEST_ID_REUSED with no change; different requestId + identical body → two entries; a replay returns the committed result after the driver is deactivated while a new requestId gets 422; invalid fields and requestId → 422 with field keys and no receipt; missing or other-vehicle person → 422 workerPersonId; driver session with owner kind → 403; vehicle session with X-Target-Vehicle → 403 TARGET_HEADER_NOT_ALLOWED; staff without or with an unknown target → error, 404 TARGET_VEHICLE_NOT_FOUND; no session → 401; a revoked session cannot read an old result; inactive vehicle → 401 for vehicle sessions (no create, no replay), 403 TARGET_INACTIVE_FOR_WRITE for staff; inactive business → no replay; another actor or vehicle scope with the same requestId never reaches the old result; a failing INSERT in work_entries, work_entry_revisions, admin_audit or mutation_receipts leaves no row in any of them and the same requestId later succeeds once; a held write lock → 503 SERVICE_UNAVAILABLE within busy_timeout, then one entry; a committed entry survives closing and reopening the database. work-entries-read-update-routes.test.ts (integration, implemented T3.5): a driver edits a pending entry → 200, version + 1, still pending, recomputed figures, one UPDATE + revision (update) + receipt, no audit and no cash confirmation; a staff edit → exactly one admin_audit row with before/after daily fields and version and no secrets, plus a platform_user revision; an owner session edits its own entry (share 0, not_required, person fixed) and staff edits it with audit; client-sent share, status, kind and business fields dropped; a driver switches to another selectable driver, an unselectable one → 422 workerPersonId; stale version → 409 VERSION_CONFLICT with nothing changed, checked before no-change; confirmed entry → 409 ENTRY_CONFIRMED even with the right version; workType different from the entry's kind → 422 workType, an equal one ignored, an invalid one 422; identical values → 422 fields.change and invalid fields 422 with keys, no receipt; an invalid body on an unknown entry → 404; same requestId + same body replays without a second revision, also after the person was deactivated and the entry confirmed; same requestId with a different body, version, entry or a create receipt → 409 REQUEST_ID_REUSED; two concurrent edits of one version → one 200, one 409, one revision; a driver edits only today's entry (yesterday 403, owner/staff allowed) and cannot move the date off today (422 date, the owner can); a driver cannot edit the owner's entry and no session edits another vehicle's entry (404); an entry whose driver was deactivated is invisible to drivers (404); vehicle session with X-Target-Vehicle 403, no session 401, staff without a target refused; a failing UPDATE, revision, audit or receipt write keeps the old entry and the same requestId later succeeds once; Istanbul midnight with an injected clock: 23:59 editable, 00:01 403; GET /:id returns the POST 201 keys for driver, owner and staff, an unknown id and another vehicle's entry share 404 WORK_ENTRY_NOT_FOUND, K1 hides owner entries and unselectable persons from drivers, no session 401; GET list: a driver without or with an unselectable workerPersonId → 422 with the same text, with a valid one only that person's driver entries; the owner sees every own-vehicle entry incl. owner kind, newest day first, never another vehicle's; keyset paging with limit 2 returns every entry once and a null nextCursor on the last page; invalid limit, cursor or workerPersonId → 422 with keys; an empty list → 200 with a null nextCursor. work-entries-confirm-route.test.ts (integration, implemented T4.1): the owner confirms a pending driver entry → 200, confirmed, version 2, receivedCents exactly as sent, one revision + one confirmation + one receipt and no audit; the received amount may differ from the remainder (0 included) and is never filled from it; admin and support confirm with X-Target-Vehicle → platform_user actor on behalf of the owner and exactly one admin_audit row without secrets; missing, numeric, empty, negative, fractional or too-large receivedCents → 422 fields.receivedCents with no write, an invalid requestId/version → 422 and malformed JSON 4xx; client-sent status, share and businessId ignored; driver session 403, no session 401, vehicle session with X-Target-Vehicle 403; other vehicle, other business and unknown id → the same 404 body; owner-kind entry → 422 CONFIRMATION_NOT_REQUIRED, not hidden by an invalid amount; same requestId + body replays 200 without a second confirmation, a different body or a PATCH/create requestId → 409 REQUEST_ID_REUSED; stale version after a PATCH → 409 VERSION_CONFLICT; a confirmed entry → 409 ENTRY_CONFIRMED with the current version, 409 VERSION_CONFLICT with an old one and PATCH 409 ENTRY_CONFIRMED; two different confirmations of one version → one 200, one 409 VERSION_CONFLICT, one row; 20 concurrent identical requests → all 200, one row; PATCH vs confirmation race → one wins; fault injection per write leaves the entry pending at version 1 with no row and the same requestId later succeeds once; GET /:id and the list return confirmation null before and an object after, POST/PATCH responses null, and a confirmation row of an older version is never attached. work-entries-correct-and-confirm-route.test.ts (integration, implemented T4.3): only the received amount 6.000 → 6.100 → 200, gross/share/expected unchanged, version 3, one new revision + one new confirmation, earlier rows byte for byte unchanged and a single confirmation in the current view; a person and date change appears under the new person and date in GET and the workerPersonId list with the share recomputed; admin and support correct with a platform_user actor on behalf of the owner and exactly one admin_audit row without secrets; client-sent status, share and kind ignored; driver session 403 with no write, no session 401, vehicle session with X-Target-Vehicle 403; other vehicle, other business and unknown id → the same 404 body; a pending driver entry → 409 ENTRY_NOT_CONFIRMED and an owner entry → 422 CONFIRMATION_NOT_REQUIRED, both with no write; workType owner → 422 fields.workType; an inactive, other-vehicle, owner or unknown person → 422 fields.workerPersonId; invalid receivedCents variants → 422 fields.receivedCents; invalid daily fields → 422 and the received amount is never filled from the remainder; identical fields and received amount → 422 fields.change while a changed amount is a correction; invalid envelope 422, malformed JSON 4xx; stale version → 409 VERSION_CONFLICT with the canonical text and PATCH on the confirmed entry still 409 ENTRY_CONFIRMED; same requestId + body replays 200 without a second version or confirmation, a different body → 409 REQUEST_ID_REUSED, a create/PATCH/confirm receipt's requestId → 409 REQUEST_ID_REUSED; 20 concurrent identical requests → all 200, one new confirmation and one receipt; two different corrections of one old version → one 200, one 409 VERSION_CONFLICT; fault injection per write keeps the old entry and confirmation intact and the same requestId later succeeds once; work-entries-history-route.test.ts (integration, work-entry history): the owner reads revisions in version order, v1 create with no change list and later versions with only the changed fields; confirmations at 6.000 (v2) and 6.100 (v3) are separate items and only v3 is current; an unconfirmed entry has only the create revision and no confirmation, and a PATCH adds an update with its changes; staff incl. support read the same history with X-Target-Vehicle and query parameters are ignored; staff rows carry username, role and the on-behalf owner, owner and driver rows access + plate with no person name; the body carries no password, hash, token, CSRF value, session or credential id or raw snapshot; driver session 403, no session 401, vehicle session with X-Target-Vehicle 403, staff with an unknown target 404 TARGET_VEHICLE_NOT_FOUND; another vehicle's, another business's and an unknown entry get the same 404 body; deactivating or renaming the person and deactivating the vehicle drop no history row and the name comes from the current people row; a legacy or malformed snapshot (missing keys, broken JSON) causes no 500; row counts are unchanged after the call Confirmer summary (staff on-behalf confirmation change): work-entries-confirm-route.test.ts — the confirmation keys are actor, confirmedAt, entryVersion and receivedCents, actor { kind vehicle_credential } for the owner; a support confirmation returns { kind platform_user, username } only and later owner GET and list reads carry the same actor with amounts unchanged, a driver GET carries only actor { kind platform_user } (no username key), and no actor_session_id, actor_platform_user_id or token appears in any response; a staff replay returns the same actor with one confirmation and one admin_audit row; another vehicle's X-Target-Vehicle → 404 WORK_ENTRY_NOT_FOUND with no write. work-entries-correct-and-confirm-route.test.ts — an owner correction's actor is vehicle_credential; a support correction makes the current version's actor the staff user (not the earlier owner confirmation), the owner GET is equal, the driver GET carries only actor { kind platform_user }, no id leaks, a replay is identical with one new confirmation and one audit row; another vehicle's target → 404 with no write. work-entries-read-update-routes.test.ts — staff confirmation → platform_user + username, owner confirmation → vehicle_credential, pending → confirmation null, in GET /:id for owner and staff and in the owner list; the driver GET and the driver K1 list carry only actor { kind } (platform_user for the staff confirmation, vehicle_credential for the owner's) and never the staff username. work-entries-driver-delivery.test.ts (integration, implemented T4.6, real migrated SQLite file + seed): after an owner confirmation of 600000 the driver GET shows expected 620000 and received 600000 apart with actor { kind } only; after a staff confirmation the driver GET and K1 list carry actor { kind platform_user } with no username key and the username appears nowhere in the response; after an owner correct-and-confirm the driver GET shows only the new version's expected/received amounts and entryVersion; driver sessions get 403 FORBIDDEN on confirm, correct-and-confirm and history with no row written (also for an owner-kind entry and another vehicle's entry id); a driver PATCH of today's confirmed entry → 409 ENTRY_CONFIRMED with no write; K1: another selectable person's driver entry → 200, an owner-kind entry, an inactive person's entry and another vehicle's entry → the same 404 WORK_ENTRY_NOT_FOUND. work-entries-list-filters.test.ts (integration, day-by-day filters): period=month&date returns only [2026-09-01, 2026-10-01), newest day first, with correct boundary days; period=week starts on Monday and period=year covers the year; only period or only date fills the missing one with its default, neither gives no date bound; the status filter combines with workerPersonId and the period; many entries on one day paged with limit=2 return every entry exactly once, the same set as limit=100; a cursor pointing to a day outside the period leaks no out-of-period row; an invalid period, date or status → 422 whose fields name only that key; another vehicle's or business's person → 200 with an empty list and a vehicleId parameter is ignored; a driver session still needs workerPersonId and sees only driver-kind entries under the filters.
- Report module (src/server/usecases/reports, vehicle period report implemented) — reports-vehicles-route.test.ts (integration): one driver + one owner entry give totals, duration and work days with the received total 0 before confirmation; an empty period returns zeros and "0" strings; workDays counts distinct work dates; an overnight entry counts on its start day; the range is half-open (startDate included, nextStartDate excluded); a week query on a Sunday starts on the previous Monday; a negative remainder stays negative; a gross total above 2^53 returns as an exact decimal string; the received total is 620000 after a 6.200 confirmation and 600000 after 6.000, the owner entry's remainder never counted; after correct-and-confirm only the new version's amount counts; a date change moves the entry to the new period; ?vehicleId / ?businessId cannot select another vehicle or business; driver 403, no session 401, vehicle session with X-Target-Vehicle 403; staff with X-Target-Vehicle reads the target's totals, unknown target 404; Cache-Control private, no-store; period=day, an empty period, 2026-02-30, 2026-9-1 or an empty date → 422 naming only that field; no parameters → 200 with the month period. report-period.test.ts: weeks start on Monday (Sunday → previous Monday, Monday is its own start) and cross month and year ends; month ranges are half-open incl. December → January and February in leap and non-leap years; year range; an invalid calendar day → null. reports-people-route.test.ts (integration): two ranges on one day give entryCount 2, workDays 1 and 480 minutes; a driver and an owner entry give two rows, the owner row isOwner with share 0 while the vehicle report keeps 1140 minutes; the body matches the contract shape (cent fields as strings, counters as numbers); an empty period returns people []; a renamed person stays one row under the new name; two people with the same name stay two rows; a deactivated person and a closed assignment keep their row and totals; a corrected entry moves to the new person/period and counts once; a gross total above 2^53 returns exactly; a negative remainder stays negative; detail totals cover the whole period while entries come newest first with a keyset cursor across pages; an unknown id, another business's owner, another vehicle's driver and an out-of-period person all get the same 404 NOT_FOUND body; driver 403, no session 401, vehicle session with X-Target-Vehicle 403 (TARGET_HEADER_NOT_ALLOWED on the list), staff with X-Target-Vehicle reads the target, untargeted staff is rejected; ?vehicleId / ?businessId ignored; invalid period, date, cursor or limit (0, 101, 5abc) → 422 naming only that field; Cache-Control private, no-store. The day-by-day list has no report use case; its filters are covered under the work/delivery module (work-entries-list-filters.test.ts); reports-summary-route.test.ts (integration, owner summary): plate, owner name, period and the PRD example totals with 0 received before and 600000 after a 6.000 confirmation; the totals equal /reports/vehicles for the same period; an empty period returns zeros with the header filled; the response carries only the expected keys (no id or business field); ?vehicleId / ?businessId cannot select another vehicle or business; driver 403, no session 401, vehicle session with X-Target-Vehicle 403 TARGET_HEADER_NOT_ALLOWED, staff with X-Target-Vehicle reads the target and an unknown target is 404; Cache-Control private, no-store; period=day, an empty period, an impossible, unpadded or empty date → 422 naming only that field; no parameters → 200 with the month period; S5.5 report consistency suites on a shared real-SQLite harness (tests/integration/report-harness.ts: real migrations + seed, route modules called with Request, no mocks or :memory:): reports-scope-matrix.test.ts — summary, vehicle report, people list, person detail and the /work-entries list against no session 401, a logged-out session 401, driver 403 on the report routes (the list is readable by drivers only with a selected person, K1), a vehicle session sending X-Target-Vehicle 403 TARGET_HEADER_NOT_ALLOWED, an unknown staff target 404 TARGET_VEHICLE_NOT_FOUND, ?vehicleId / ?businessId never widening the scope, a staff target returning only that business's data, no error body carrying another customer's name or cent amount, the same 404 body for unknown and out-of-scope person ids, and Cache-Control private, no-store; reports-prd-scenario.test.ts — the PRD §4 example (driver 14 Sep, owner 15 Sep, remainder total 14.400 TL; received 0 → 6.200 → 6.000 while the owner's work never adds to the received amount) returns identical bodies on all five reads for the owner session and for staff with X-Target-Vehicle, two entries on one day give entryCount 2 and workDays 1, and a person and date correction never double-counts the old revision or confirmation; reports-snapshot.test.ts — with a second real connection committing a correct-and-confirm in between, the owner summary and the person detail stay wholly on the old snapshot, the vehicle report, people list and day-by-day list are single SELECTs that leave no transaction open, and the received total counts only the current version's confirmation; reports-query-plan.test.ts — every report and list use case's own SQL (captured, not hand-copied) searches work_entries through an idx_work_entries_* index with no full scan, the ratchet catches an unindexed query, and perf:reports exists but is outside ci-steps.json and test:integration
- Data access (src/server/data) — db.test.ts pragmas (WAL, foreign_keys, synchronous FULL, busy_timeout), schema.test.ts, db-connection, app-db-access, sqlite_version ≥ 3.51.3; app-db-startup.test.ts (integration): missing file → MissingDatabaseFileError without creating it, empty SQLite file or empty __drizzle_migrations → UninitializedDatabaseError (not PendingMigrationsError) naming the path, no table created and connection closed on the error path, migrated DB opens with §3.6 PRAGMA values; db-init-cli.test.ts (integration): plain mode creates + migrates and is idempotent, --existing exits 1 on a missing path or an empty file without creating anything, succeeds on an initialized DB, unknown argument exits 1; db-connection.test.ts write-transaction metrics: all fields 0 with no sample, counted without changing the return value and reset on read, an fn error rethrown as the same object and counted without a lock failure, SQLITE_BUSY while another connection holds the write lock rethrown unchanged and counted in lockFailures, a nested call (SAVEPOINT) not counted separately, nearest-rank p99 with count and max exact beyond the sample capacity; work-entry-crash-integrity.test.ts (integration, child process on a real migrated SQLite file): create, confirm and correct_and_confirm each SIGKILLed inside the open BEGIN IMMEDIATE transaction leave the old complete state and SIGKILLed after COMMIT leave the new complete state; integrity:check passes after every kill and a retry with the same requestId and body writes exactly once (first write, or replay)
- systemd jobs / release scripts — release-shared.test.ts, release-build.test.ts, ci-workflows.test.ts; deploy-config.test.ts (unit): app service runs as non-root from the release root, listens only on 127.0.0.1:3000, reads /etc/dolmus-takip, derives APP_ORIGIN from DOLMUS_DOMAIN, only the DB directory writable, restart limits equal ARCHITECTURE §8.2, enabled via multi-user.target; Caddy drop-in reads the same domain.env; env examples carry only the allowed keys; SERVER-SETUP guide paths, --existing on release switch and 'not tried' markers checked; guide opens with '## Değişkenler' exporting DOLMUS_DOMAIN, AWS_PROFILE, AWS_REGION, DOLMUS_INSTANCE, DOLMUS_STATIC_IP_NAME, DOLMUS_SSH_KEY, no 12-digit account id after it and every aws lightsail command carries --profile/--region; step order create-instances → allocate-static-ip → put-instance-public-ports → DNS dig → scp → Node 24 → Caddy → db-init → create-first-admin → systemctl start; only ports 22/80/443, never 3000; first admin runs as the service user with --password-stdin and no --password argument; manual verification table has 33 rows (9 install + 7 health automation + 5 backup + 3 maintenance gate + 3 release tool + 6 load acceptance, incl. a separate load DB path and the mandatory return to the production DB before the pilot) all marked 'to be verified during the manual install'; 'PİLOT İÇİN HAZIR DEĞİL' present and no ISSUE-29 reference in the guide, README, RELEASE.md or deploy/ files; health units: timer every 30 s enabled in timers.target, oneshot service with only the state directory writable, flock -n guard, script under /opt/dolmus-takip/health independent of current, TimeoutStartSec above the app's 90 s stop timeout; app unit AssertPathExists=! on the recovery lock in [Unit]; health scripts use only node: built-ins and execFile, state and lock under /var/lib (no /run or /tmp); journald drop-in persistent, 200 MB; every new deploy file carries the 'prepared, not tried' header; SERVER-SETUP installs scripts, units and the journald limit and enables the timer after the services; OPS.md carries the F13 login counter and hash queue rows, the lock removal procedure and the Caddy access line content; backup/restore scripts planned M6; release-build.test.ts, release-apply.test.ts and db-backup.test.ts read the expected migration count from drizzle/meta/_journal.json instead of a fixed number
- Quality gate CLI (scripts/quality-gate.ts, scripts/lib/quality-gate.ts; npm run quality-gate) — quality-gate.test.ts (unit): the gate is exactly typecheck → lint → test:unit → test:integration in that order; a record is valid only when its tree, step list and Node version equal the values computed now — another tree, another Node version or an unsupported record version is invalid; recordPathFor accepts only a git object id (no path escape); a missing or corrupt record file is invalid without throwing; the content, not the file name, is validated (another tree's record copied under this tree's name is invalid); writeRecord creates the directory, leaves no temp file and overwrites an existing record; recordBlocker refuses a record when the tree was dirty at the start, became dirty during the gate or HEAD moved; runQualityGate in a temporary git repo with fake steps writes a valid record only when every step passes on a clean tree, stops at the first failing step without running later ones or writing a record (also when only the last step fails), treats an unrunnable step as a failure, and on a dirty tree still runs the steps but writes no record. ci-workflows.test.ts: quality-gate is the single first ci-steps.json step, before test:release and release:build, and package.json maps it to node scripts/quality-gate.ts. release-build.test.ts (release project, tests/release): the clone starts with a fixture record written by writeRecord for its own tree (it never reads the root record and never runs the gate in setup) and must pass readRecord before any test runs; release:build prints the skip line and runs no gate step for a recorded tree, also for an --allow-empty second commit with the same tree; a failing unit-test commit has no record, runs the full gate, stops and writes no record
- Backup CLI (scripts/db-backup.ts, scripts/lib/backup-schedule.ts) — backup-schedule.test.ts (unit): Istanbul wall clock across the day boundary, 00:00 not 24:00, offset ISO; protected window 02:55–04:00; copyDeadline (same-day 02:55, expired inside the window, 25-min budget for a manual off-window run); decidePublish protected_window / deadline_passed; UTC stem names sorting by time, own dot-prefixed temp names only; planRetention keeps the newest 2 manifest-complete copies, never counts a copy without a valid manifest and never touches foreign files; parseManifest; findRowCountDrops on guarded tables only (sessions may shrink); formatLogLine single-line, 120-char cap. db-backup.test.ts (integration, real SQLite): copy + manifest match; published copy opens alone read-only with no -wal/-shm and full data; commits still in the WAL are in the copy and the live DB/WAL are untouched; retention keeps exactly 2 and leaves foreign files; stale own temp files removed; rejection keeps the previous two copies byte-identical (publish in 02:55–04:00, guarded row drop, foreign_key_check failure, share/remainder mismatch), 04:00 publishes; missing DB or backup dir is never created; invalid DOLMUS_BACKUP_NOW, unknown subcommand and extra args rejected; status exits non-zero with no copy, lists release ids without changing the dir, counts a hash mismatch and a manifest-less copy as bad. deploy-config.test.ts: backup timer 02:30 Europe/Istanbul with Persistent=false; oneshot as dolmus-takip from /opt/dolmus-takip/current; ExecStart flock -w 600 -E 75 on /var/lib/dolmus-takip/ops.lock with TimeoutStartSec above the wait; same hardening as the health unit and only data/ + backup-ready/ writable; no maintenance/recovery-lock reference and no [Install]; release-verify runs the archive's own db-backup run + status (release-build.test.ts)
- Health endpoints and server health task (src/app/api/v1/health, deploy/health) — health-ready-route.test.ts (integration): migrated DB → 200 {status:'ok'}; missing file → 503 SERVICE_UNAVAILABLE without creating it; uninitialized (empty) DB → 503; pending migration → 503; a transient failure recovers to 200 on the next call (open failure not cached); live answers 200 without touching the DB even with no DB file. health-decision.test.ts (unit): no action and no counting inside the 90 s grace, checks start at exactly 90 s; no restart on the first two live failures, one restart on the third; a success resets the counter and intermittent failures never restart; a restarted unit drops previous failures; unknown start time → no action; maintenance marker or recovery lock → no restart and no second lock; ready failing with live OK never restarts across repeated runs; at most 2 restarts in 15 min, the next need writes the lock; restarts outside the window drop out, a future timestamp still counts; start-limit-hit writes the lock regardless of maintenance and never twice; no input yields an action other than restart / write-lock and the task source contains no lock removal or start-limit reset; restartNeedsLock (start-limit error only); systemctl show parsing with unknown/null defaults; MemAvailable percent; disk 80% warn / 90% critical
- Runtime metrics (src/server/observability) — runtime-metrics.test.ts (unit): event-loop delay reported as the part above the 10 ms resolution and never negative, CPU as the difference from the previous read, each sample covers only its own interval; the line follows the health task's logfmt shape with a fixed key order, is one line and carries only numbers besides ts; one line every 60 s, a second start does not add a timer, the timer is unref'd. instrumentation-runtime-metrics.test.ts (integration): two register() calls write one line per 60 s carrying the interval's write-transaction and hash-queue counters, reset afterwards
- Page: Araç/ekip girişi — LoginForm states, messages.ts mapping, plate.ts normalization/format
- Page: Aydınlatma metni — messages.test.ts (unit): exactly seven sections numbered 1–7; section 1 carries the data-controller placeholders; processed data (names, staff usernames, plates, work and money records), purposes, collection method and legal basis, transfer, at-least-five-year retention with name anonymisation on a deletion request and no record deletion, KVKK m.11 rights and the application route; the PERSON_ANONYMIZED text and the confirm-dialog texts (irreversible, records kept)
- Page: Şoför günlük kayıt — work-time.test.ts (implemented T3.1: Istanbul 'today' independent of the device time zone around the 21:00Z day boundary; calendar day shift across month and year ends; 08:00–17:30 = 570 min with UTC instants at +03; a 00:30 start keeps workDate on the start day; explicit next-day 22:00–06:00 = 8 h; equal same-day times refused, not counted as 24 h; a same-day end before start refused instead of rolling over; next-day equal times = exactly 1440 min, longer refused; 23:59 → next-day 00:00 = 1 min; each missing or invalid field gets only its own error; non-existent dates and malformed values invalid; '14 Eylül 2026' date and '9 saat 30 dakika' duration formatting)
- Page: Şoför günlük kayıt — amounts and live summary (implemented T3.2): money.test.ts (Turkish '1.250,50' input read losslessly; explicit 0 valid, empty not 0; each invalid input gets its own text and code — negative, precision, thousands ('1.5', '10.50'), format, too large — six distinct texts; accepted up to MAX_SAFE_INTEGER cents; '6.200,00 TL' formatting with thousands dot and decimal comma, negative sign, unsafe number refused, parse/format round trip lossless; API cent string produced and parsed only as ^(0|[1-9][0-9]*)$ within the limit) and work-calculation.test.ts (driver 2000 bps on gross before expenses, owner 0; rounding 3 kuruş → 1, 2 → 0, 7 → 1, 8 → 2; negative remainder not clamped; all zero → 0; BigInt input lossless on large gross; expenses + share beyond the safe limit → AmountOutOfRangeError; negative or unsafe input refused; rule v1 constants) — the same calculation drives the page preview and the server recomputation
- Page: Sahip çalışma kaydı / ekip adına çalışma kaydı — work-entry-ui.test.ts (implemented T3.3: driver-format rows returned as is; from the management view only an active assignment + active person is selectable; two same-named people stay two rows carrying only id and name; an empty list is valid (empty state) and a malformed response returns null; a malformed active flag on a management row rejects the whole list instead of making the person selectable)
- Page: Şoför günlük kayıt / Sahip çalışma kaydı / ekip adına çalışma kaydı (save, implemented T3.4; result screen and unknown-result resolution, implemented S3.6) — work-entry-ui.test.ts: the draft name is keyed by vehicle id; an empty draft carries today's date and the given requestId and is not pending; any field, another date or a pending send makes the draft dirty; buildWorkEntryBody sends cents as decimal integer strings, workerPersonId only for driver kind, a trimmed note only with a used expense, never person/business/role/share fields, an explicit 0 gross, and returns null for no kind, an invalid amount or a note with an empty amount; classifyWorkEntryResponse treats 201 + expected body as created, network error, unreadable body, 5xx and a malformed 201 as unknown, and 4xx as definite carrying the code and only string field errors; workEntryErrorMessage maps 401/403/409 to their texts and never prints the server message for an unknown code; shouldReleaseAfterError releases on every definite error at a first attempt, and after an earlier attempt only on 422 and 409 REQUEST_ID_REUSED (401, every 403 code, 404 and others keep the draft pending); hasEarlierAttempt counts a pending draft written before the attemptSent flag existed as sent; S3.6: a 201 whose entry lacks a string startsAt or endsAt is unknown, not created; formatWorkTimeRange gives the Istanbul wall-clock 'HH:MM–HH:MM' and appends '(ertesi gün)' when the end falls on the next Istanbul day, including a range that stays on one UTC day; savedEntryFromDetail maps a re-read entry (confirmed status included) to the result data; canResolveUnknown resolves only a pending draft with a frozen body and a sent attempt while online and not disabled; draftAfterCreated clears and draftAfterRelease releases (new requestId, fields kept) only a draft that still belongs to the resolved requestId, never another tab's newer draft; workEntryLoginHref returns the fixed /giris for vehicle roles and /yonetim/giris for staff, with no parameters. messages.test.ts pins the result and connection texts to the approved screen texts ('Bağlantı yok. Henüz kaydedilmedi.', 'Kaydın sonucu kontrol ediliyor.', 'Kaydedildi', 'Yenile', 'Kaydı aç', 'Başka bir çalışma kaydı gir', the owner hint and the '<name> · <plate>' / '<date> · <range>' lines) and checks that the unknown-result texts never say 'Henüz kaydedilmedi' and the refresh error never claims deletion
- Page: Kayıt detayı ve düzenleme / Kayıtlarım (driver, owner, staff; implemented T3.6) — work-entry-ui.test.ts: the edit draft is keyed by vehicle + entry and separate from the create draft; workEntryDetailHref per mode with the vehicle id in the staff URL; parseWorkEntryDetail reads a valid entry without extra keys and returns null for a malformed one; editDraftFromEntry builds a clean draft with Istanbul times, TL texts and the base version, incl. an overnight end and an other expense; a clean draft is not dirty, any change or a pending send is, surrounding whitespace is not a change; releaseEditDraft drops the frozen body with a new requestId keeping the values and version, rebaseEditDraft binds the values to the current version; canEditEntry: confirmed never, owner/staff any unconfirmed entry, driver only today's pending entry; buildWorkEntryPatchBody sends the draft's version and requestId, no kind/business/role/share/status fields, no workerPersonId for owner kind, the other expense only when used, null for an invalid amount or a note with an empty amount, and stable JSON; classifyWorkEntryUpdateResponse treats 200 + valid entry as saved and network error, unreadable body, 5xx and a malformed 200 as ambiguous; shouldReleaseAfterUpdateError releases on every definite error at a first attempt and afterwards only on 422 and 409 REQUEST_ID_REUSED / VERSION_CONFLICT / ENTRY_CONFIRMED (401/403/404 keep the draft pending), updateErrorNeedsReread only on VERSION_CONFLICT and ENTRY_CONFIRMED; VERSION_CONFLICT gets the canonical conflict text, ENTRY_CONFIRMED and 404 their own; editPersonOptions adds the entry's inactive person as '(pasif)' and offers no other inactive person; parseWorkEntryList rejects the whole list on one malformed entry and buildWorkEntriesUrl encodes person and cursor. work-time.test.ts: istanbulWallClock (UTC+3, an end past midnight on the next day, round trip with evaluateWorkTime). Cash confirmation (implemented T4.2) — work-entry-ui.test.ts: parseWorkEntryDetail accepts confirmation null or { receivedCents, confirmedAt, entryVersion, actor } and rejects a missing, mistyped or malformed one; the confirmer summary keeps { kind vehicle_credential } and { kind platform_user, username } (extra keys such as role dropped), while a missing, unknown or malformed actor (null, string, array, {}, unknown kind) becomes null without rejecting the entry, platform_user without a non-empty string username is kept as { kind platform_user } (the driver view) without inventing a username, a confirmed status with a null confirmation rejects the body, and reads a negative remainderCents (K5) while every other amount must be non-negative; receivedPrefill fills the expected hand-over only for a pending driver-kind entry and stays empty for a negative expected amount, an owner entry and a confirmed or not-required entry, and its text parses back to the same cents; frozenReceivedText turns the frozen body's amount back into field text and returns null for no, malformed or invalid body; receivedDifference gives the shortfall or excess in cents, nothing for equal, invalid or empty input, also against a negative expected amount; buildWorkEntryConfirmBody always sends an explicit decimal-integer receivedCents with stable key order and returns a field error without a request for empty, negative or malformed input; classifyWorkEntryConfirmResponse counts only a 200 with status confirmed and a confirmation as success, an unconfirmed or malformed 200, network error, unreadable body and 5xx as ambiguous, and a definite error carries status, code and field texts; shouldReleaseAfterConfirmError releases on every definite error at a first attempt and afterwards only on errors that prove nothing was written; confirmErrorNeedsReread only on VERSION_CONFLICT and ENTRY_CONFIRMED; the confirm draft name starts with the edit-draft prefix and differs from the edit draft, an empty draft is untouched and a release gives a new requestId, keeps the typed amount and drops the frozen body. money.test.ts: parseSignedApiCents accepts a negative cent string and returns null for '-0', malformed text and a magnitude beyond the limit Correct-and-confirm (implemented T4.3) — work-entry-ui.test.ts: the correction draft name starts with the edit-draft prefix and differs from the edit and confirm drafts; its received text is the current confirmation's amount, not the expected hand-over, and the draft is bound to the entry's version; a gross change leaves the received amount as it is, and a gross-only or received-only change makes the draft dirty; without a confirmation the received field is empty and a pending send is always dirty; release gives a new requestId, keeps the values and drops the frozen body, rebase also moves the version; buildWorkEntryCorrectBody carries the daily fields plus an explicit receivedCents (incl. a used other expense and note) and builds nothing for an invalid received amount or gross; classifyWorkEntryCorrectResponse counts only a 200 with status confirmed and a confirmation of the current version as success, and a confirmation of an older version, an unconfirmed, pending or malformed 200, network error and 5xx as ambiguous, while a definite error carries status, code and field texts; shouldReleaseAfterCorrectError releases on every definite error at a first attempt and afterwards only on errors that prove nothing was written (incl. 409 ENTRY_NOT_CONFIRMED); correctErrorNeedsReread only on VERSION_CONFLICT and ENTRY_NOT_CONFIRMED. messages.test.ts pins the correct-and-confirm texts and the shared success text 'Kayıt düzeltildi ve onaylandı.' Driver delivery status (implemented T4.6) — work-entry-ui.test.ts: deliveryStatusView gives a pending entry 'Teslim edilecek tutar', 'Henüz doğrulanmadı' and the hint with no confirmation lines; the hint appears only in driver mode and only while pending; a confirmed entry keeps expected and received apart ('6.200,00 TL' / '6.000,00 TL') with the Istanbul confirmation time '14 Eylül 2026 · 18:00'; the support trace is 'Sahip adına platform desteği' without a username in driver mode, with the username for owner/staff and absent for vehicle_credential; a confirmation is shown only for status confirmed; a negative expected amount reads '-50,00 TL'; owner kind uses 'Giderlerden sonra kalan' and 'Onay gerekmiyor'; deliveryStatusLabel maps the three statuses to one text each.
- Page: Kayıt geçmişi (owner and staff) — work-entry-history.test.ts (diffWorkEntryRevision: identical values → empty list; only changed fields with before/after in the fixed field order; the person compared by id and shown by name, a rename under the same id is no change; a missing receivedCents counts as null, so a confirmation lists null → value and a correction value → value; a note null ↔ text transition is listed while duration and status changes are not) and work-entry-history-ui.test.ts (buildHistoryRows: chronological creation, an edit with before → after and two separate confirmation rows with only the last current; v1 carries no changes and the received amount is not repeated in change lines; a correct-and-confirm that also changed another field lists that field in its own row before the confirmation row; no row carries a total; currentHistorySummary takes the last revision's values and only the current confirmation, received null without one and null without revisions; staff actor '<username> · <role>', vehicle credential access + plate with no person name, the on-behalf text only on staff rows; a staff confirmation row carries the support trace 'Sahip adına platform desteği · <username>' (— for an empty username) and a vehicle confirmation row none; field values BigInt-safe, day and time in Istanbul, empty → —, malformed amount → —)
- Page: Sahip raporları and Sahip özeti (implemented) — report-ui.test.ts: range text for a month, a week crossing a month or a year end, a year and a leap February; parseVehiclePeriodReport reads a valid body into BigInt cents, reads a total above 2^53 exactly, lets only the remainder be negative and returns null for malformed bodies; vehiclePeriodReportView builds the remainder, received total, breakdown, duration and work days from the S5.2 data, takes the previous/next anchors from the server period (previous = the day before startDate, next = nextStartDate), keeps the minus sign of a negative remainder, formats a total above 2^53 exactly and is empty with no forbidden wording when there are no entries; buildVehiclePeriodReportUrl sends date only when given. money.test.ts: parseApiTotalCents has no upper bound, accepts negatives and returns null for -0 and malformed text; report-ui.test.ts (person report): parsePeoplePeriodReport and parsePersonPeriodReport read valid bodies into BigInt cents (an empty list is valid), read totals above 2^53 exactly, let only the remainder be negative and return null for malformed bodies; buildPeoplePeriodReportUrl sends date only when given, buildPersonPeriodReportUrl escapes the id and adds cursor only when given; peoplePeriodReportView builds 'duration · days · entries', the 'Mal sahibi' label and gross/share per card, is empty for an empty list and never sums person days into the vehicle's days; personDetailView builds the totals, the entry rows and the owner entry link, keeps a negative remainder's sign, formats a gross above 2^53 exactly and uses no forbidden wording; report-ui.test.ts (day-by-day): buildDailyEntriesUrl carries period and date and nothing else without filters and adds workerPersonId, status and cursor only when given (an empty person or status is not added); parseDailyEntriesPage reads the entries and nextCursor and returns null for a malformed body, a malformed entry or an empty nextCursor string; dailyEntryCardView shows the expected hand-over and no received amount for a pending driver entry, takes the received amount from the confirmation without replacing the expected one for a confirmed entry, uses 'Onay gerekmiyor' with no hand-over or waiting wording for an owner entry and keeps the minus sign of a negative remainder; report-ui.test.ts (owner summary): buildOwnerSummaryUrl sends date only when given; parseOwnerSummary reads the summary body into BigInt cents with the server's period, accepts a negative remainder and returns null for a negative gross, a malformed amount, a missing field, a non-report period kind, a body without summary or no body; ownerSummaryView builds the range text, the labelled totals (Hasılat, Mazot, Diğer masraf, Şoför payı, Hesaplanan kalan) from the PRD example, the received amount separately, the start day and the previous/next anchors, and is empty for entryCount 0 with no forbidden wording; report-ui.test.ts (staff support mode): personDetailView and dailyEntryCardView given a target vehicle id link every entry to /yonetim/araclar/:id/kayitlar/:entryId with the same vehicle id on each row, and without it keep the /sahip/kayitlar/:id links
- Page: Şoförlerim (owner and staff) — drivers-ui.test.ts: name trim/whitespace collapse and 1–120 limit, active/inactive split keeping same-named people as separate rows, similar-name hint ignoring Turkish case and matching word prefixes, stale-draft detection per operation (pending never stale), request bodies carry no person/business/vehicle/role ids and escape URL segments, error codes mapped to screen texts (5xx → generic connection text, only 5xx ambiguous), exact F3 warning and rename-note texts, affected-vehicle plate display; KVKK: the anonymize request goes to the business-scoped admin endpoint with exactly requestId + version, a retry after an unknown result rebuilds the same URL and body from the stored draft, a changed person version makes a non-pending draft stale, ids are URL-escaped, no request is built without businessId, rowNameActions offers anonymisation only with permission and neither action on an anonymised row, 409 PERSON_ANONYMIZED maps to its screen text
- Page: Yönetim / işletme formları — use-stored-draft version freeze on unknown result, 422 text; design tokens contrast (contrast.test.ts)
- Page: Yönetim / araç formları — stale draft detection against the server version, pending result never treated as stale (draft-version.test.ts)
- Page: Yönetim araması — admin-search.test.ts (active filter parsing, business vs vehicle list URL, encoded query and cursor, bare /yonetim link on defaults, 401/403/5xx/network/422 error texts); search-fold.test.ts (Turkish case folding, NFD equals NFC, empty string kept)
- Page: Destek alanı — support-target.test.ts (clears only the target vehicle's four drafts incl. the work-entry draft and its work-entry edit drafts by prefix, never another vehicle's, storage errors never throw, draft names equal the existing form names)
- Page: İşlem geçmişi — audit-ui.test.ts (all 27 written actions, incl. work_entry.create 'Çalışma kaydı oluşturuldu' and work_entry.update 'Çalışma kaydı düzenlendi', have a label different from the raw code, business/vehicle status labels distinct from person labels, unknown code shown raw, creation detection, staff actor with username + role label, vehicle credential shown as access + plate without a person name, before/after rows only for changed fields with ids/version hidden, password reset makes no creation claim, password-like keys never shown even if returned, object values serialized as text, Istanbul time, URL building; team-account actions carry distinct Turkish labels, a created account shows no before value, the role is shown with its Turkish label and a role change lists only the changed field, password/hash-like keys stay hidden; person.anonymize shows 'Kişi adı anonimleştirildi' with 'Anonimleştirildi' Hayır → Evet and no previous name)
- Page: Ekip hesapları — team-users-ui.test.ts (username trimmed + lower-cased, 3–32 allowed characters, spaces and Turkish characters refused; full-name whitespace collapse and 1–120 limit; passwords never trimmed, whitespace-only accepted, empty and >200 chars refused with their own texts; create body carries canonical values and platformRole but no actor role or id; update diff returns only changed fields and an empty name on a nameless account is no change; the info PATCH body is built from the frozen draft with its requestId and version and is identical after reload; reset body carries only requestId + newPassword; the account id travels only in the URL, escaped; per-account draft names; session-ended URL /yonetim/giris?oturum=bitti; status codes mapped to screen classes, 'Değişiklik yok' vs generic 422 text, the raw server message never shown, only known field errors picked; role labels equal the staff header labels)
- Release apply CLI (scripts/release-apply.ts) — release-apply.test.ts (integration): deploy order marker → stop → old release's pre-migration copy → db-init --existing → current → live/ready/financial check → traffic, and each failure (copy, ready, live, start-limit-hit, fingerprint change, migration changing totals) keeps the marker and opens no traffic; preconditions change nothing (recovery lock, release already current, manifest mismatch, foreign maintenance, --under-maintenance without a marker, ops lock held → 75, non-0700 state dir, unresolved previous release, missing current); F5 takeover: after fingerprint_mismatch plain deploy is refused as previous_release_unresolved with state.json untouched, --under-maintenance publishes the fix, keeps the old bytes as a 0600 taken-over file and cleanup keeps the inherited copy until mark-verified; an unreadable state refuses plain deploy and the flag without a marker, and after takeover cleanup deletes no copy or release until mark-verified; rollback --code refused after migrations or with an unknown count and returns to old code otherwise; rollback --code-and-db refused after traffic, on a fingerprint change or an unreadable state, otherwise moves DB/WAL to preserved/, revokes sessions and returns to the old schema and release; a retry after a failure following install resumes without a second install and keeps the same preserved dir, and a retry whose live DB matches neither recorded fingerprint is refused keeping the record; mark-verified/cleanup retention rules; inspect is read-only and reports missing migrations as check_failure
- Load-acceptance CLIs (scripts/load-seed.ts, scripts/load-run.ts, scripts/lib/load-*.ts) — load-client.test.ts (unit): writes send Origin, JSON content type, CSRF and cookie and never X-Forwarded-For, reads only the cookie; latency from the real send until the full body; session cookie taken and cleared; unknown write results (timeout, dropped connection, 503) retried with the same body and requestId, 409 REQUEST_ID_REUSED stays unexpected, exponential backoff until retries run out. load-metrics.test.ts (unit): nearest-rank percentiles; response classes (concurrency 409s expected, 429 counted separately, write timeouts/5xx unknown); open-model schedule for ramp and waves; ledger reconciliation finds lost, duplicate and inconsistent entries and accepts either state of an unknown result; verdict boundaries (save p95 2000 ms and report p95 3000 ms pass, unexpected must stay below 1%, a single 429 to a legitimate user, any lost/duplicate/inconsistent entry or an incomplete reconciliation fails); acceptance eligibility needs ≥ 30 min sustained, a non-loopback target, 100 users and the release schema. load-run-args.test.ts (unit): defaults 300/300/1800 s, 100 users, 20 s timeout; timeout ≤ 12 s refused; load:seed and load:run exist and no CI step, workflow or test:* script runs them. load-run-probe.test.ts (unit): the reachability probe is GET /giris without cookie or CSRF, 404/503/connection errors named, no request under /api/v1/health, the report claims no DB readiness. load-seed.test.ts (integration): migrations applied and ≥ 5 years of history ending before the load window, side-file counts/commit/migration hashes equal the DB and repo, entries in the app's shape, credentials file 0600 and passwords absent from the side file, vehicle and staff credentials log in through the real login routes; refuses an existing DB, WAL, credentials or side-file path, NODE_ENV=production and a credentials file inside the repo
- Integrity check CLI (scripts/integrity-check.ts) — integrity-check.test.ts (integration): the receipt-operation map equals the use cases' operation names; a clean seeded DB exits 0 with every violation count 0 and the file unchanged; each injected violation (revision gap, confirmation on a missing version, two confirmations per version, 'confirmed' without confirmation, share/remainder mismatch, receipt without target, confirmed driver entry back to 'pending', owner entry awaiting confirmation, driver-role confirmation, confirmation on a non-confirming revision) exits 1 as integrity_violations naming the violation; a committed row still in the live WAL is seen and neither the main file nor the WAL is written; no argument or a missing file exits 1 without creating a file
- Pilot readiness guides (docs/QA-PLAN.md §2–§3, docs/SERVER-SETUP.md §5.1) — pilot-guides.test.ts (unit): the acceptance traceability table has exactly one row per story (35) and per PRD §9 item (28) in order, each with named evidence, a status and a candidate-commit result, no row marked passed before a candidate commit is chosen, every test file it names exists; the load procedure names the three commands and three separate scenarios, warmup ≥ 5 min, ramp, sustained ≥ 30 min and ≥ 15 min between login-heavy runs, the targets and the separately counted refusals, the source of RAM, CPU/burst, event-loop, disk, WAL and SQLite-wait readings, and an outside probe via Caddy on GET /giris with health endpoints kept on localhost; the S6.6 row stays open only because the target machine is missing; the load DB steps never seed the production path, point only the app service at the load DB, keep the health timer running and remove the drop-in before the pilot

_Existing test files enumerated from src and tests/; planned targets from QA-PLAN critical details. T2.2 added src/server/auth/vehicle-password.test.ts, src/lib/draft-version.test.ts, tests/integration/admin-vehicles-routes.test.ts and race cases in session-usecases / vehicle-login-route tests. T2.3 added tests/integration/reset-vehicle-password-route.test.ts. S2.4 added tests/integration/drivers-routes.test.ts, tests/integration/drivers-affected-vehicles.test.ts and src/lib/drivers-ui.test.ts. The admin support/audit change added tests/integration/admin-list-search-routes.test.ts, tests/integration/admin-audit-route.test.ts, src/server/usecases/admin-audit/sanitize.test.ts, src/lib/admin-search.test.ts, src/lib/search-fold.test.ts, src/lib/support-target.test.ts and src/lib/audit-ui.test.ts. S2.6 added tests/integration/admin-users-routes.test.ts and src/lib/team-users-ui.test.ts, and extended tests/integration/session-usecases.test.ts (platform session race cases) and src/lib/audit-ui.test.ts (team-account rows); tests/integration/release-build.test.ts now expects 4 migrations. T3.1 added src/lib/work-time.test.ts for the pure K3 date/time rule in src/lib/work-time.ts (evaluateWorkTime, istanbulToday, addDays, formatWorkDate, formatDuration); the server-side re-validation of the same rule stays in the planned Work/delivery module targets. T3.2 added src/lib/money.test.ts, src/lib/work-calculation.test.ts and src/server/usecases/work-entries/work-entries.test.ts; the share-rounding, overflow, negative-remainder and server re-validation targets that were planned under the Work/delivery module are now implemented, and the client preview math for the driver page is covered by the shared calculation tests. T3.3 added src/server/usecases/work-entries/subject.test.ts, tests/integration/work-entry-prepare-create.test.ts (prepareWorkEntryCreate, findSelectableDriver through it, readVehicleOwnerPerson) and src/lib/work-entry-ui.test.ts (selectableFromDriversResponse for the owner and staff pages). T3.4 added tests/integration/work-entries-create-route.test.ts (createWorkEntry through the real route on a real migrated SQLite file, incl. fault injection per INSERT and a held write lock), extended src/lib/work-entry-ui.test.ts with the save helpers (draft, body, response classification, error texts, release rule), and updated src/lib/audit-ui.test.ts (25 written actions) and src/lib/support-target.test.ts (four drafts per vehicle). T3.5 added tests/integration/work-entries-read-update-routes.test.ts (updateWorkEntry, readWorkEntryForScope and listWorkEntriesForScope through the real routes on a real migrated SQLite file, incl. fault injection per write and an injected clock). T3.6 extended src/lib/work-entry-ui.test.ts with the edit and list helpers, src/lib/work-time.test.ts with istanbulWallClock, src/lib/support-target.test.ts with edit-draft clearing and src/lib/audit-ui.test.ts to 26 written actions. S3.6 (T3.7 in code) extended src/lib/work-entry-ui.test.ts with formatWorkTimeRange, savedEntryFromDetail, canResolveUnknown, draftAfterCreated / draftAfterRelease, workEntryLoginHref and the startsAt/endsAt requirement of a created response, and src/lib/messages.test.ts with the result-screen and unknown-result texts. T4.1 added tests/integration/work-entries-confirm-route.test.ts (confirmWorkEntry through the real route on a real migrated SQLite file, incl. fault injection per write and concurrency). T4.2 extended src/lib/work-entry-ui.test.ts with the confirmation helpers and src/lib/money.test.ts with parseSignedApiCents. src/lib/audit-ui.test.ts still covers 26 written actions; the new work_entry.confirm audit action has no label. T4.3 added tests/integration/work-entries-correct-and-confirm-route.test.ts (correctAndConfirmWorkEntry through the real route on a real migrated SQLite file, incl. fault injection per write and concurrency) and extended src/lib/work-entry-ui.test.ts with the correction helpers and src/lib/messages.test.ts with the correction texts; audit-ui.test.ts is unchanged and the work_entry.correct_and_confirm audit action has no label. The work-entry history change added tests/integration/work-entries-history-route.test.ts (readWorkEntryHistoryForScope through the real route on a real migrated SQLite file), src/lib/work-entry-history.test.ts and src/lib/work-entry-history-ui.test.ts; messages.test.ts and audit-ui.test.ts are unchanged. The staff on-behalf confirmation change extended tests/integration/work-entries-confirm-route.test.ts, work-entries-correct-and-confirm-route.test.ts and work-entries-read-update-routes.test.ts with the confirmer-summary cases, src/lib/work-entry-ui.test.ts with the actor parsing of parseWorkEntryDetail and src/lib/work-entry-history-ui.test.ts with formatHistorySupportTrace; messages.test.ts and audit-ui.test.ts are unchanged. The driver delivery-status change (T4.6) added tests/integration/work-entries-driver-delivery.test.ts, changed the driver confirmer expectations in work-entries-confirm-route.test.ts, work-entries-correct-and-confirm-route.test.ts and work-entries-read-update-routes.test.ts from actor null to { kind } only, and extended src/lib/work-entry-ui.test.ts with deliveryStatusView / deliveryStatusLabel and the new actor and confirmed-without-confirmation parsing; messages.test.ts is unchanged. The vehicle period report change added tests/integration/reports-vehicles-route.test.ts, src/lib/report-period.test.ts and src/lib/report-ui.test.ts, and extended src/lib/money.test.ts with parseApiTotalCents. The person period report change added tests/integration/reports-people-route.test.ts and extended src/lib/report-ui.test.ts with the person parsers, URL builders and view models; report-period.test.ts and money.test.ts are unchanged. The daily-entries change added tests/integration/work-entries-list-filters.test.ts and extended src/lib/report-ui.test.ts with the day-by-day URL builder, page parser and card view; money.test.ts and report-period.test.ts are unchanged. The owner summary change added tests/integration/reports-summary-route.test.ts and extended src/lib/report-ui.test.ts with the owner summary URL builder, parser and view model; money.test.ts and report-period.test.ts are unchanged. The report consistency / query-plan change (S5.5) added tests/integration/report-harness.ts, reports-scope-matrix.test.ts, reports-prd-scenario.test.ts, reports-snapshot.test.ts and reports-query-plan.test.ts (the ratchet uses scripts/lib/query-plan.ts), and extended src/lib/report-ui.test.ts with the staff-mode entry links; money.test.ts and report-period.test.ts are unchanged. Server setup prep added tests/unit/deploy-config.test.ts, tests/integration/app-db-startup.test.ts and tests/integration/db-init-cli.test.ts; both folders are already in the vitest unit/integration projects, so CI runs them without a pipeline change. The T6.2 manual-install change extended tests/unit/deploy-config.test.ts with the SERVER-SETUP variables, step-order, ports, first-admin and manual-verification-table checks; no new test file. The crash/freeze health change added tests/unit/health-decision.test.ts, tests/integration/health-ready-route.test.ts and tests/integration/admin-hash-queue-log.test.ts, and extended tests/unit/deploy-config.test.ts, tests/integration/platform-login-route.test.ts, vehicle-login-route.test.ts and vehicle-login-hash-queue.test.ts; all sit in the existing vitest unit/integration projects (vitest.config.mts), so CI runs them without a pipeline change. The daily backup change added tests/unit/backup-schedule.test.ts and tests/integration/db-backup.test.ts, and extended tests/unit/deploy-config.test.ts (backup unit/timer, shared lock with the release switch, 21-row manual verification table, backup install order, Lightsail snapshot 00:00 UTC, release manifest cleanup rule, OPS backup records) and tests/integration/release-build.test.ts ('db-backup run/status çalıştı'); all sit in the existing vitest unit/integration projects, so CI runs them without a pipeline change. The release-apply change (commit bc4dc78) extended tests/integration/release-apply.test.ts with the F5 takeover cases (unresolved after fingerprint_mismatch, unreadable state) and the rollback --code-and-db retry cases (resume without a second install, refusal when the live DB matches neither fingerprint); the file sits in the existing vitest integration project, so CI runs it without a pipeline change. The KVKK change added tests/integration/person-anonymize-route.test.ts and src/server/usecases/drivers/person-name.test.ts, plus KVKK cases in src/lib/drivers-ui.test.ts, src/lib/messages.test.ts, src/lib/audit-ui.test.ts (27 actions) and src/server/auth/permissions.test.ts (17 permissions); the release and backup tests now read the migration count from the journal. All sit in the existing vitest projects, so CI runs them without a pipeline change. The load/integrity change added src/server/observability/runtime-metrics.test.ts, tests/integration/instrumentation-runtime-metrics.test.ts, tests/integration/integrity-check.test.ts, tests/integration/work-entry-crash-integrity.test.ts (child: tests/integration/work-entry-crash-child.ts), tests/integration/load-seed.test.ts, tests/unit/load-client.test.ts, tests/unit/load-metrics.test.ts, tests/unit/load-run-args.test.ts, tests/unit/load-run-probe.test.ts and tests/unit/pilot-guides.test.ts, and extended src/server/auth/hash-queue.test.ts (interval stats), tests/integration/db-connection.test.ts (write-transaction metrics) and tests/unit/deploy-config.test.ts (33-row manual verification table); all sit in the existing vitest unit/integration projects (vitest.config.mts), so CI runs them without a pipeline change. The quality-gate-record change added tests/unit/quality-gate.test.ts and a ci-workflows.test.ts case, and extended tests/release/release-build.test.ts (record reuse in the clone, skip line, no record for a failing tree); the unit files sit in the existing vitest unit project (tests/unit/**) and the release file in the release project, so CI runs them without a pipeline change. The fixture-record change makes the tests/release/release-build.test.ts clone write its own .quality-gate record (writeRecord with QUALITY_GATE_RECORD_VERSION, the clone's HEAD^{tree} and HEAD, qualityGateStepIds, the running Node version) instead of copying the root record or running quality-gate in beforeAll; the record lives only in the throwaway clone and is removed in afterAll; SETUP_TIMEOUT_MS drops from 20 min to 2 min, and the full gate now runs only in Test 4 (the unrecorded failing-unit-test tree). The skip-worktree git-view helper (tests/release/git-view-snapshot.ts) and its unit test tests/unit/git-view-snapshot.test.ts were removed._

## CI/CD Integration

### Pipeline flow

**Repos:** dolmus-takip

PR opened / push (ci.yml, ubuntu-24.04, Node from .nvmrc, 30 min timeout, concurrency cancel):
  npm ci → quality-gate (npm run quality-gate: typecheck → lint → test:unit → test:integration, stops at the first failure; when every step passes and the working tree stayed clean with HEAD unchanged, writes the gitignored record .quality-gate/<HEAD^{tree}>.json with tree, commit, Node version, step ids and passed_at; on a dirty tree the steps run but no record is written) → test:release (release:build/verify meta-test in a temporary clone; the clone starts with a fixture .quality-gate record written for its own tree, never reads the root record, and runs the full gate only in the failing-unit-test tree scenario) → test:e2e (next build + standalone, Chromium + WebKit) → release:build (dirty tree rejected; skips the gate only when a record matches the current tree, step list and Node version exactly, otherwise runs the full gate itself and stops before building on the first failure; sqlite_version gate, secret scan, tarball + manifest) → release:verify (hash, platform, db-init, the archive's own scripts/platform-admin.ts create-first-admin on the same temp DB with the password on stdin, then the archive's own scripts/db-backup.ts run + status on that temp DB with a fixed off-window clock and exactly one published manifest, /api/v1/health/live 200).
  Artifacts kept 14 days; Playwright report uploaded only on failure (7 days). No secrets used.
Merge to main: same pipeline; merge only when CI is green (2026-09-19 decision).
Release (release.yml, workflow_dispatch with ref): the same chain (quality-gate → test:release → test:e2e → release:build → release:verify); builds and verifies the release tarball, artifact 30 days; no deploy step. Deployment to Lightsail is a manual SSH procedure following docs/SERVER-SETUP.md (T6.2 manual-install decision; T6.5).
Local parity: npm run ci:local executes scripts/ci-steps.json in the same order; tests/unit/ci-workflows.test.ts asserts both lists match.

_Step list single-sourced; runner.temp moved from job env to step level (commit e516c15). release-verify.ts runCreateFirstAdmin (T6.2): a missing file in platform-admin's import tree fails release verification instead of only on the server. release-verify.ts runBackupCommands (daily backup change): the archive's own db-backup run and status run with DOLMUS_BACKUP_NOW fixed at 2026-01-15T12:00Z so the 02:55–04:00 window never fails verification by time of day; a missing file in the backup import tree fails release verification instead of only on the server. Quality-gate record (scripts/lib/quality-gate.ts, scripts/release-build.ts ensureQualityGate): the gate steps are defined only in QUALITY_GATE_STEPS; the record is keyed by the git tree hash, not the commit SHA, and is validated against the tree, step ids and process.version computed now, so a changed step list or Node version invalidates old records and a corrupt record falls back to the full gate. The record is an accident guard, not tamper protection: anyone who can write to the checkout can forge it, and there is no key to sign it with._

### Run frequency

**Repos:** dolmus-takip

| Test type | When |
| --- | --- |
| Quality gate: typecheck, lint, unit, integration (npm run quality-gate) | Every push and PR (ci.yml) and on release.yml dispatch, first step; locally via ci:local or directly. release:build re-runs it only when no valid .quality-gate record exists for the current tree. Only npm run quality-gate or release:build's own full gate on a clean tree write that record; separate typecheck/lint/test:unit/test:integration runs leave none, so running them before release:build runs the full gate twice (release:build prints this hint right after its no-record line); test:release does not read that record (its clone starts with its own fixture record) and runs the full gate only in its failing-tree scenario |
| Release pipeline meta-test (npm run test:release: release:build/verify end to end in a temporary clone) | Every push and PR (ci.yml) and on release.yml dispatch, right after quality-gate; locally via ci:local; outside test:integration |
| E2E Chromium + WebKit | Every push and PR in CI; Chromium-only at package gates inside the DIJJI runtime container |
| Release build + verify | Every push/PR and on manual release.yml dispatch |
| Report query-plan measurement (npm run perf:reports: 5-year synthetic data, EXPLAIN QUERY PLAN + timings of every report query, health latency while reports run) | Manual only — outside ci-steps.json and test:integration; the result is committed to docs/REPORT-QUERY-PLAN.md. The small-data index ratchet (reports-query-plan.test.ts) runs with integration on every push and PR |
| Regression + real phone checks | End of each milestone M1–M5 |
| Ops drills: crash/freeze, backup/restore, migration rollback | M6 on isolated Linux and target Lightsail |
| Load test (npm run load:seed on the target, then npm run load:run from a separate machine: login-burst, active-mix and write-peak, 100 users each, ≥ 30 min sustained) | M6 before pilot, on the target machine with 5-year representative data in a separate load DB; manual only — outside ci-steps.json, CI workflows and test:* (load-run-args.test.ts asserts it) |
| Integrity check (npm run integrity:check, read-only) | Manual — after every load scenario, on a backup copy of the load DB; also run by work-entry-crash-integrity.test.ts with integration on every push and PR |
| Post-release smoke | After every controlled release, on a dedicated test business |
| Restore drill | Before and after first release, then monthly |

_QA-PLAN §3 run plan + implemented CI triggers. perf:reports (package.json, node --import ./scripts/lib/ts-resolver.mjs scripts/report-query-plan.ts) builds a temporary SQLite file with the real migrations and 5 years of synthetic data (PERF_VEHICLES default 40), writes docs/REPORT-QUERY-PLAN.md and fails on a work_entries full scan or when GET /api/v1/health/live, served in the same Node process while reports run, reaches the 3-second health timeout (F12). reports-query-plan.test.ts asserts the command is absent from ci-steps.json and test:integration. load:seed / load:run (package.json, node --import ./scripts/lib/ts-resolver.mjs) state in their headers that they are outside ci-steps.json, test:unit and test:integration; tests/unit/load-run-args.test.ts asserts no CI step, workflow or test:* script runs them. integrity:check (node scripts/integrity-check.ts) runs on a single read snapshot, so its header places it after a load scenario or on a backup copy; docs/SERVER-SETUP.md §5 row 32 checks db-backup run followed by integrity:check event=integrity_passed after each run. The quality-gate-record change (scripts/ci-steps.json, ci.yml, release.yml) folded typecheck/lint/unit/integration into the single quality-gate step; release:build reuses its tree-keyed .quality-gate record instead of re-running the gate for the same content; the test:release clone writes its own fixture record instead of reading it. test:release is the separate vitest release project (vitest.config.mts) and runs as its own step after quality-gate._

## Bug Tracking

### Labels

**Repos:** dolmus-takip

- severity:critical — financial data loss/double count, cross-business leak, wrong confirmation, irreversible corruption; blocks release
- severity:high — core entry/confirmation/login unusable, authorization bypass, unrestorable backup; blocks release
- severity:medium — non-blocking functional gap; impact + workaround written; related AC stays open
- severity:low — cosmetic/secondary text; not low if it harms readability or money meaning
- status:planned / not-run / passed / failed / awaiting-decision
- trace:QAxx / Sx.y / ACn

_No tracker is mandated; the plan does not create GitHub issues._

### Bug template

**Repos:** dolmus-takip

QA group / Story / AC: QAxx · Sx.y/ACn
Severity: critical | high | medium | low
Area:
Steps to reproduce:
Expected:
Actual:
Environment / versions: commit, Node, SQLite, package versions, browser/OS, fixture seed
Evidence: sanitized logs (with request_id) / screenshots
Owner:
Fix:
Re-test result: reproducing test + relevant regression run

_Closing requires the reproducing test and regression to pass, not just a code change._

## Manual Test Checklist

### Manual Test Checklist

**Repos:** dolmus-takip

- Android Chrome + iPhone Safari: open link, log in, pick person, enter times/amounts, single Kaydet; measure 30–60 s target with representative users
- 320 px width, 200% zoom, long Turkish names (İ/ı/Ş/ğ), light background; amounts and buttons readable, no blocking horizontal overflow
- Touch targets ≥ 48 px, numeric/time keyboards, visible focus and keyboard order
- VoiceOver / TalkBack: labels, field errors and status announcements
- Slow network, drop during save, retry, phone switch: no false success, no duplicate entry, unknown result understandable
- Shared phone: after logout the next driver sees no previous data or preselected name
- Owner distinguishes expected 6.200 TL vs received 6.000 TL; after Düzelt ve onayla totals and history not confused; staff action visible as on-behalf
- Empty period, pending/confirmed entries, inactive driver, two people with the same name are understandable
- Staff: first admin created on the real server from the release archive with scripts/platform-admin.ts create-first-admin as the service user, password read from stdin (--password-stdin) and never typed on the command line or kept in shell history; re-run is idempotent; reset-admin-password recovers access; new owner/driver password handed to the customer over WhatsApp and login succeeds with it
- Real HTTPS via Caddy: Secure cookie set, CSP not breaking pages, maintenance response during release
- Timing: the server install and the 33 server install checks below are run manually by the team at the end, during the manual install; none is verified yet and the system is not ready for the pilot until all are recorded
- Server install check (SERVER-SETUP §5 row 1): certificate obtained — valid certificate for the domain, Caddy log shows it was obtained
- Server install check (SERVER-SETUP §5 row 2): certificate renewal — expiry recorded, automatic renewal observed near expiry and logged
- Server install check (SERVER-SETUP §5 row 3): HTTP → HTTPS — 301/308 with Location https://…
- Server install check (SERVER-SETUP §5 row 4): APP_ORIGIN = https://$DOLMUS_DOMAIN — ExecStart derives it; staff login through the domain works with the first admin
- Server install check (SERVER-SETUP §5 row 5): health endpoint closed from outside — /api/v1/health/live 404 from outside, 200 on localhost
- Server install check (SERVER-SETUP §5 row 6): port 3000 unreachable from outside — listens on 127.0.0.1 only; only 22/80/443 public
- Server install check (SERVER-SETUP §5 row 7): DB, backup and secret files not served — 404 for every such path; files owned by dolmus-takip, 0750/0640
- Server install check (SERVER-SETUP §5 row 8): auto start after reboot — services enabled, then active after reboot; a trial record is still listed
- Server install check (SERVER-SETUP §5 row 9): data kept across a release switch — current points to the new release; trial record and login still work
- Server install check (SERVER-SETUP §5 row 10): health timer runs every 30 s — each run logs event=metrics, event=probe and event=decision action=none
- Server install check (SERVER-SETUP §5 row 11): crash → systemd restarts after 10 s; the health task does not restart; no lock written within the start budget
- Server install check (SERVER-SETUP §5 row 12): freeze → after 3 failed runs exactly one event=corrective_restart; the frozen process is killed after the 90 s stop timeout and comes back active
- Server install check (SERVER-SETUP §5 row 13): ready failing alone never restarts — reason=ready-failing-live-ok, no restart; ready 200 and healthy once the DB file is back (no empty DB created)
- Server install check (SERVER-SETUP §5 row 14): exhausted restart budget → persistent recovery.lock; the app does not start even after reboot; the lock is removed only by the manual OPS §5-B procedure
- Server install check (SERVER-SETUP §5 row 15): maintenance marker blocks restarts — reason=maintenance, no restart; checks resume once the marker is removed
- Server install check (SERVER-SETUP §5 row 16): Caddy access log without headers, query/cursor values or remote_port; client IPs masked /16; journald stays within ~200 MB
- Server install check (SERVER-SETUP §5 row 17): shared ops lock opened by the backup unit under its sandbox — lock root:dolmus-takip 640; a held lock makes the unit wait and fail with 75 after at most 600 s without copying
- Server install check (SERVER-SETUP §5 row 18): daily copy unit runs as dolmus-takip — exit 0 while the app runs; copy + manifest under backup-ready; refused inside the 02:55–04:00 window
- Server install check (SERVER-SETUP §5 row 19): backup timer 02:30 Europe/Istanbul with no catch-up — next trigger 23:30 UTC; a missed run after reboot is not replayed
- Server install check (SERVER-SETUP §5 row 20): Lightsail automatic snapshot 00:00 UTC (03:00 Türkiye) — at least one snapshot Success; real start/finish times recorded
- Server install check (SERVER-SETUP §5 row 21): copy–snapshot relation — the newest copy was published before the snapshot started (before 23:55 UTC); otherwise no new successful DB backup is declared
- Server install check (SERVER-SETUP §5 row 22): maintenance gate — with the marker present, pages and API answer 503 with Retry-After: 120 from Caddy without reload; health still 404 outside, 200 locally
- Server install check (SERVER-SETUP §5 row 23): maintenance gate reopens — removing the marker restores normal responses and login without a Caddy reload
- Server install check (SERVER-SETUP §5 row 24): maintenance gate cannot stay open — directory owners/modes as specified and the caddy user can stat the marker; otherwise the install stops
- Server install check (SERVER-SETUP §5 row 25): release tool order — maintenance, pre-release copy of the old release, migration, traffic; 503 during the release, normal after event=release_traffic_opened; trial record kept
- Server install check (SERVER-SETUP §5 row 26): release tool: DB rollback before traffic opens — event=restore_installed; the new DB/WAL preserved, not deleted; current is the previous release; old sessions revoked; phase=rolled_back
- Server install check (SERVER-SETUP §5 row 27): release tool: DB rollback refused after traffic opens — reason=traffic_opened, exit 1; service stays active, no maintenance marker, DB untouched
- Server install check (SERVER-SETUP §5 row 28): load DB on a separate path — its sha256 equals the side file; app.sqlite untouched; load:seed never run on the target
- Server install check (SERVER-SETUP §5 row 29): only the app service uses the load DB — process env points to load-test/load.sqlite, ready 200; backup and health units stay on the production DB
- Server install check (SERVER-SETUP §5 row 30): three load scenarios run separately — three reports with acceptance_eligible=true and the release-candidate commit; ≥15 min apart; health task on
- Server install check (SERVER-SETUP §5 row 31): process kill during write-peak — active again after ~10 s; 0 lost / duplicate / inconsistent entries; time and recovery recorded
- Server install check (SERVER-SETUP §5 row 32): integrity after every run — db-backup run exit 0 and integrity:check event=integrity_passed
- Server install check (SERVER-SETUP §5 row 33): return to the production DB and removal of the load data (mandatory before the pilot) — process env points to app.sqlite, ready 200, no load-test in the unit, trial record listed, load-test directory and generator credentials deleted

_All 33 rows of the server setup guide's manual verification table (SERVER-SETUP §5: 9 install + 7 health automation + 5 backup + 3 maintenance gate + 3 release tool + 6 load acceptance, asserted as 33 rows by tests/unit/deploy-config.test.ts) are added per the product owner's correction; the guide states the machine carries no real customer data until the table is completed and recorded. The first-admin item now follows the packaged procedure in SERVER-SETUP §3.4 (scripts/platform-admin.ts from the archive, --password-stdin, run as dolmus-takip with app.env). Product owner note: the server install and these checks are done manually by the team at the end._
